# MARKETING_FIXES — Прояв · робочий список виправлень

> **Призначення.** Живий перелік маркетингових/UX-проблем сайту з готовими інструкціями для виправлення. Створено для передачі в **Claude Code**: бери пункт зі статусом `OPEN`, виконуй за інструкцією, постав `DONE` і дату.
> **Джерело істини — `marketing_context/MARKETING_CONTEXT.md`.** Якщо інструкція тут суперечить конституції — пріоритет у конституції; познач конфлікт і запитай власника.
> **Цей файл доповнюється ітераціями** (див. «Журнал ітерацій» унизу). Не переписуй старе — додавай і онови статус.

## Правила виконання (guardrails — обов'язкові)
1. **Не вигадувати соц-доказ** — жодних відгуків, цифр, імен клієнтів, яких немає насправді (конституція 8.5, 11).
2. **Не додавати токени/кольори/шрифти поза `tokens.css`** (11).
3. **Не обіцяти фотографу корекцію його кольору** — лише «контроль друку · колір недоторканний» (Розділ 4).
4. **Без клікбейту:** знижки, «терміново», оклики, капс, псевдоціни (5, 11).
5. **Ціни — лише з `config.js`** (+ дзеркало `functions/_utils.js`). Не хардкодити.
6. Пункти з міткою **[РІШЕННЯ ВЛАСНИКА]** не виконувати самовільно — спершу узгодити: вони чіпають бізнес-рішення або вимагають реального контенту.
7. Голос: тихо, тепло, точно, з повагою. Перед будь-яким текстом — Протокол (12) і Лінтер (5).

## Легенда
- **Пріоритет:** `P0` блокер · `P1` висока цінність · `P2` поліш · `P3` опційно.
- **Статус:** `OPEN` · `DONE` · `WONTFIX` · `ASK` (треба рішення власника).
- **Тип:** `КОПІРАЙТ` · `UX` · `ДОВІРА` · `КОНСИСТЕНТНІСТЬ` · `КОНТЕНТ`.

---

## 📊 Зведена таблиця

| ID | Пріоритет | Тип | Сторінка | Статус |
|----|-----------|-----|----------|--------|
| M-01 | P1 | ДОВІРА/КОНТЕНТ | весь сайт | OPEN · ASK |
| M-02 | P1 | КОПІРАЙТ/UX | index | OPEN |
| M-03 | P1 | КОПІРАЙТ/UX | nabir | OPEN |
| M-04 | P1 | ДОВІРА/КОПІРАЙТ | index, partner, oferta | OPEN · ASK |
| M-05 | P1 | КОПІРАЙТ | index:835 | OPEN · ASK |
| M-06 | P2 | КОПІРАЙТ | index (Перша серія) | OPEN · ASK |
| M-07 | P2 | КОНСИСТЕНТНІСТЬ | index, order, status | OPEN |
| M-08 | P2 | ДОВІРА | index (відгук-блок) | OPEN |
| M-09 | P3 | КОПІРАЙТ | status.html | OPEN · ASK → див. M-14 |
| M-10 | P2 | КОНСИСТЕНТНІСТЬ | admin | ПЕРЕГЛЯНУТО (іт.2) — дизайн уже на бренді; лишились шаблони сповіщень |
| M-11 | P1 | ДОВІРА | config.js (legal/payment) | OPEN · ASK |
| M-12 | P3 | UX | account (деталь) | OPEN |
| M-13 | P3 | КОПІРАЙТ | index (Архів) | OPEN |
| M-14 | P2 | КОПІРАЙТ | server/transactional (Telegram + сповіщення) | OPEN · ASK |
| M-15 | P2 | КОНСИСТЕНТНІСТЬ | docs (constitution §13 + WORKLOG) | OPEN |
| M-16 | P2 (висока цінність) | UX/АНАЛІТИКА | index (CTA без data-track) | OPEN |
| M-17 | P2 | ТОКЕНИ/КОНСИСТЕНТНІСТЬ | order, partner, admin (зелений поза палітрою) | OPEN · ASK |
| M-18 | P3 | ДОВІРА/ТОЧНІСТЬ | privacy.html §10 | OPEN |
| M-19 | P2 | БРЕНД/КОНСИСТЕНТНІСТЬ | status.html ← _utils PRODUCT_NAMES | OPEN |
| M-20 | P3 | A11Y/КОПІРАЙТ | order.html (alt прев'ю кадрів) | OPEN |
| M-21 | P3 | АДАПТИВ | privacy.html `.lg-table` (≤320px) | OPEN · live-перевірено |
| M-22 | P3 | АДАПТИВ | account.html `.pv-author` (≤375px) | OPEN · live-перевірено |

---

## 🔴 Найвищий важіль (довіра + позиціонування)

### M-01 · Вакуум довіри: на сайті немає жодної людини й нуль реального соц-доказу — `P1` · `ASK`
**Де:** весь сайт (особливо `index.html` геро + блок «відгук» ~987–997).
**Проблема:** фейковий соц-доказ правильно прибрано, але нічим реальним не замінено. Уся довіра тримається на заявах про ремесло + Epson + гарантії. Для невідомого преміум-бренду в малому місті це головний бар'єр (конституція 2.2 «недовіра до невідомого преміум-бренду»; 6 «справжні (не фейкові!) кейси»).
**Чому важливо:** це найбільший важіль конверсії, і він повністю чесний — будується з реального контенту.
**Інструкція:**
1. Додати на лендінг **ноту/обличчя засновника** — коротка щира історія «хто за цим стоїть і навіщо». Для персональної майстерні майстер = носій довіри. (Потрібен текст + фото від власника → `[РІШЕННЯ ВЛАСНИКА]`/`КОНТЕНТ`.)
2. **Уточнення після іт.2:** продуктові фото в галереї — РЕАЛЬНІ (`assets/products/` — envelopes/box/newborn, справжнє пакування), тож це не «фейк». Прогалина точкова: немає кадрів **процесу/майстерні** (Epson, руки, конверт у роботі), **обличчя засновника** і **реального клієнтського набору**. Додати саме ці типи. Бонус: у `assets/products/` є **невикористані** реальні фото (envelopes-02/03/05/06, box-02/03/04/06, newborn-02…12) — ними можна збагатити сторінки замість CSS-градієнт-мокапів.
3. Закласти механіку **перших реальних кейсів** (з дозволом клієнта) — на майбутнє, коли підуть замовлення.
**Не робити:** не генерувати вигадані відгуки/цифри/імена. Якщо реального контенту ще немає — лишити чесний стан і повернутись.

### M-02 · Гарантія «відбиток збереже задум» ніде не названа як гарантія біля CTA — `P1`
**Де:** `index.html` (геро ~697–698; філософська картка 745–748; FAQ 1024–1027 — розпорошено).
**Проблема:** Північна Зоря бренду («Прояв гарантує, що відбиток збереже задум») і обіцянка передруку подані дрібно й розкидано. Найсильніше, найбільш «ваше» твердження не подане як іменований видимий стовп.
**Чому важливо:** named guarantee знімає ризик покупки в незнайомого бренду й прямо відображає позиціонування (конституція 1).
**Інструкція:** підняти гарантію в **окремий помітний елемент** біля головного CTA (геро та/або фінальний CTA ~1061–1073). Формулювання в межах Розділу 4: напр. «Гарантія проявлення: відбиток збереже ваш задум — інакше передрукуємо за наш кошт.» Без % і без слова «знижка». Використати наявні класи/токени.
**Не робити:** не додавати нових кольорів/токенів; не перетворювати на «акційний» банер.

### M-03 · `nabir.html` — ціль усіх партнерських посилань `?ph=`, але це голий вибір формату — `P1`
**Де:** `nabir.html` (тіло сторінки 80–104).
**Проблема:** холодний клієнт зі сторіс фотографа потрапляє на «Оберіть формат» без емоційного гачка й без видимого рядка довіри. «Колір недоторканний / гарантія передруку» є лише в OG-мета (7–13), не в тілі. Фотограф витратив свою репутацію, щоб надіслати людину — сторінка її не «ловить».
**Чому важливо:** це точка передачі, на якій тримається вся модель B2B2C (конституція 2.1, 7 «nabir DoD»).
**Інструкція:** додати **один тихий рядок** під підзаголовком (після 83) — гарантія + хто ми, напр.: «Преміальне проявлення на Epson. Ваш колір недоторканний, передрук за наш кошт.» Без додаткового кроку/тертя (11 «не ускладнювати флоу»). За наявності `?ph=` можна додати теплий рядок «Вас рекомендує ваш фотограф».
**Не робити:** не перетворювати лаконічний chooser на лендінг; максимум 1–2 рядки.

### M-04 · Заперечення «а раптом просто не сподобається» закрите лише як дефект — `P1` · `ASK`
**Де:** `index.html` FAQ 1022–1028; `partner.html` econ-FAQ 164–165; підстава — `oferta.html` 9.1 (товар на індивідуальне замовлення обміну/поверненню не підлягає).
**Проблема:** і лендинг, і кабінет фотографа відповідають лише про дефект («тон поплив / папір повівся»). Жива тривога іншого роду — «а раптом не те, що уявляв» — не зустрінута прямо, а юридично повернення немає. Для покупки наосліп у незнайомого бренду це діра в довірі.
**Чому важливо:** risk-reversal — класичний важіль конверсії; зараз він однобокий.
**Інструкція:** ухвалити позицію разом із власником і назвати тривогу тепло. Варіанти: (а) жест на перший набір («якщо щось не так — передрукуємо або підберемо рішення, поговоримо»); (б) чесний абзац «що ми гарантуємо, а що ні» без обіцянок понад оферту. Узгодити з `oferta.html` 8–9, не обіцяти зайвого.
**Не робити:** не обіцяти безумовне повернення коштів усупереч оферті.

---

## 🟠 Суперечності з конституцією (порушують власний закон бренду)

### M-05 · `index.html:835` показує «−15%» — порушення Лінтера + неузгодженість із order.html — `P1` · `ASK`
**Де:** `index.html:835` (`<strong id="fs-discount">−15%</strong>`) і JS `index.html:1178` (`disc.textContent = '−' + d.discountPct + '%'`).
**Проблема:** Розділ 5 прямо забороняє показ дисконту/відсотка в преміумі. При цьому `order.html:3245` ту саму механіку подає on-brand — «Застосовано ціну "першої серії"» (без %, без «знижка»; коментар-обґрунтування `order.html:3243–3244`). Одна механіка — два голоси.
**Чому важливо:** це єдине вціліле порушення Лінтера на видимих поверхнях + явна внутрішня неузгодженість.
**Інструкція:** привести `index.html` до голосу order.html — подати як **ціновий рівень**, без числа: напр. «Ціна першої серії» / «Умови першої серії — для перших десяти наборів». Прибрати `−X%` з тексту (і з JS-ін'єкції 1178). Економію клієнт усе одно бачить на екрані успіху сервер-авторитетно.
**Статус ASK:** саму акцію санкціонувало `[РІШЕННЯ ВЛАСНИКА]` (ТЗ ч.2). Міняємо лише **подачу**, не механіку — але підтвердити формулювання.

### M-06 · Дефіцит-тиск у «Першій серії» («Залишилось N з 10», «Зайняти місце») — `P2` · `ASK`
**Де:** `index.html:837` («Зайняти місце →»), `index.html:838,1180` («Залишилось N з N»).
**Проблема:** скарсіті-лічильник — м'яка терміновість. Розділ 11: «Не нав'язувати й не тиснути. Рекомендуємо і чекаємо.» Розділ 5 проти «терміново/поспішайте».
**Чому важливо:** уся відмінність бренду — у відсутності тиску; навіть м'який лічильник працює проти позиціонування.
**Інструкція:** пом'якшити до запрошення без тиску — напр. кнопка «Залишити кадри →», а лічильник місць або прибрати, або подати спокійно («Відкрито запис на першу серію»). Узгодити з власником (механіка валідації попиту — його рішення).

---

## 🟡 Консистентність і поліш

### M-07 · SLA «48 годин» захардкоджено, хоча в конфізі є пік 72 год — `P2`
**Де:** `index.html` геро 707 + продукти 769 + процес 884; `order.html:1046` (екран успіху) — усі жорстко «48 годин». Конфіг: `config.js` `sla{standardHours:48, peakHours:72, peak:false}`. FAQ (`index.html:1041`) і `order.html:838` (`#sla-note`) пік враховують.
**Проблема:** у піковий сезон статичні поверхні обіцяють 48 год, тоді як реально 72 — overpromise, б'є по «точно» (Розділ 5) і по довірі.
**Інструкція:** зробити SLA-рядки **peak-aware** з одного джерела (`PROYAV.sla`): якщо `peak:true` → показувати `peakHours`. Мінімум — `order.html:1046` success-text і геро/процес index підставляти з конфіга, як уже зроблено для `#sla-note`. Формула голосу: «Готово за 48 годин — замовляйте будь-коли» (5).
**Не робити:** не дублювати числа в копії — лише з `config.sla`.
**Підтверджено іт.3:** `oferta.html:6.1` чесно пише «до 72 годин» у пік — тобто **юр-сторінка точніша за маркетингові**, які жорстко кажуть 48. Узгодити в бік чесного «48, у пік до 72».

### M-08 · Блок «відгук» досі має візуальну граматику соц-доказу (аватар-кружок) — `P2`
**Де:** `index.html:990–996` (`.testimonial-avatar` — порожній градієнт-кружок + ім'я + роль).
**Проблема:** копія тепер чесна (підпис «Прояв · наш задум»), але порожній аватар імітує людину, якої немає. Залишковий патерн фейк-відгуку.
**Інструкція:** або (а) прибрати аватар-кружок і подати як маніфест/цитату бренду без «обличчя»; або (б) як частина M-01 — поставити туди реальне фото засновника й справжній підпис. Не лишати порожній аватар-плейсхолдер.
**Не робити:** не ставити стокове «обличчя клієнта».

### M-09 · Емодзі 🤍 у статусі замовлення — звірити з голосом — `P3` · `ASK`
**Де:** `status.html:119` («Сподіваємось, набір вас потішив... 🤍»).
**Проблема:** голос навмисно стриманий («тихо, преміум не кричить», Розділ 5). Один емодзі в момент радості захищуваний, але це свідомий вибір, не випадковість.
**Інструкція:** узгодити з власником — лишити (теплий акцент у фінальній точці) чи прибрати заради аскетичності. Якщо лишати — більше ніде емодзі не вводити.

### M-10 · `admin.html`: дизайн уже на бренді — лишився лише on-voice-шаблон сповіщень — `P2`
**ПЕРЕГЛЯНУТО в іт.2.** Перевірив `admin.html` напряму: підключає `tokens.css`, темна камера, scanline, монограма, Cormorant/Inter/JetBrains Mono, sepia/safelight, mono-uppercase нав, рольовий гейтинг. **Дизайн уже на бренді** — стара мітка «admin off-brand» застаріла (див. M-15).
**Що реально лишилось:** оператор/менеджер пише сповіщення клієнту **вільним текстом** (`admin.html:912–913` «Заголовок» / «Текст для клієнта…») без голосових підказок → ризик повідомлень поза голосом (клієнт це бачить у кабінеті).
**Інструкція:** додати 2–3 **готові on-voice шаблони** в форму відправки сповіщення («У дорозі», «Прибуло», «Потрібне уточнення по кадру») — щоб staff лишався в голосі (тихо/тепло/точно, Розділ 5). Узгодити тексти з M-14.
**Не робити:** не «уніфіковувати» дизайн адмінки — він уже на бренді.

### M-11 · Неповні юр-реквізити й порожня платіжна картка підривають довіру/ясність оплати — `P1` · `ASK`
**Де:** `config.js` `legal{ taxId:'', email:'' }` (+ адреса не задана) і `payment{ card:'', holder:'' }`.
**Проблема:** (1) на `oferta/privacy/partnerstvo` ховаються рядки реквізитів → юр-сторінки виглядають неповно (мінус довіра + юр-ризик при масштабі). (2) Порожня `card` → «як оплатити» на успіху/статусі падає в розмите «реквізити надішлемо особисто» замість конкретної суми+картки — тертя в оплаті.
**Чому важливо:** і довіра, і конверсія в оплату.
**Інструкція:** `[РІШЕННЯ ВЛАСНИКА]` — заповнити `config.legal` (taxId/email/адреса) і `config.payment.card`+`holder`. Код уже підставляє коректно; потрібні лише дані. (Дубль із WORKLOG «дії власника» — лишаю тут як маркетингово-критичне.)
**Підтверджено іт.3:** на `oferta.html:137` і `privacy.html:165` рядок «Ідентифікаційний код» ховається через порожній `taxId` → юр-сторінки виглядають неповними. Email-рядок так само прихований.

### M-12 · Кабінет клієнта не показує авторство фотографа — `P3`
**Де:** `account.html` (картка деталі замовлення `renderDetail` 850–898).
**Проблема:** підпис фотографа є на фізичній картці (8.8), але в кабінеті клієнта зв'язок «цей набір — від вашого фотографа» ніяк не підсилено.
**Інструкція (опційно):** у деталі замовлення, якщо є `photographer`, тихо показати «Кадри: [ім'я автора]». Підсилює B2B2C і повагу до автора (Розділ 5 «автор, не клієнт»). Лише якщо дані доступні в `api-client-orders`.

### M-13 · «Архів» (1200₴) для весіль обмежений 80 кадрами — звірити очікування — `P3`
**Де:** `index.html:806–807` («50–80 кадрів... Для весіль»); `config.js` large `cap:80`.
**Проблема:** весілля = сотні кадрів; cap 80 може не збігтися з очікуванням «весь архів». Кап чесно вказано, тож не критично, але формулювання «Архів» + «великі історії» може обіцяти більше.
**Інструкція (опційно):** уточнити копію, що це **кураторська добірка** (обряд, не дамп усіх фото) — підсилює преміальність замість того, щоб виглядати обмеженням. Узгодити з власником.

---

## 🟡 Ітерація 2 — серверна/транзакційна копія + дисципліна доків

### M-14 · Голос у транзакційних повідомленнях: емодзі + оклики + жаргон «пінг» — `P2` · `ASK`
**Де (усе, що бачить клієнт/фотограф):**
- `functions/api-orders.js:179` — сповіщення клієнту: «**Дякуємо!** Оплату отримано. Сподіваємось, набір вас потішив **🤍**».
- `functions/api-orders.js:17` — пінг фотографу: «…Дякуємо, що рекомендуєте Прояв **🤍**».
- `functions/tg-webhook.js:60` — «**Вітаємо у Прояв!** …».
- `functions/tg-webhook.js:86` — «**✅** Сповіщення підключено, {ім'я}**!** … отримаєте **пінг** тут щоразу… Дякуємо, що з нами **🤍**».
- `status.html:119` — «…набір вас потішив **🤍**» (це M-09).
**Проблема:** Розділ 5 — голос «тихо, без окликів, без капслоку». Тут системно: оклики («Дякуємо!», «Вітаємо!»), **два стилі емодзі** (🤍 і ✅) і **жаргон «пінг»**, що тече фотографу (тех-сленг ≠ «з повагою/точно»).
**Чому важливо:** це реальні дотики бренду (Telegram-DM + кабінет), не «внутрішня кухня». Зараз вони трохи голосніші й технічніші за лендінг.
**Інструкція:** ухвалити **єдине свідоме правило** і застосувати скрізь:
- Емодзі: лишити **лише 🤍** як один теплий знак — або прибрати всі. Прибрати ✅ (замінити словом).
- Оклики: прибрати «!» з транзакційних — «Дякуємо. Оплату отримано…», «Вітаємо у Прояв.».
- «пінг» → «сповіщення» у текстах фотографу (`tg-webhook.js:86`, і в копії кабінету `partner.html:653` «миттєвий пінг»).
**Не робити:** не вихолощувати теплоту — мета консистентність, не сухість. Зміни тексту лише після підтвердження власником голосу.
**Сильне (не плутати з проблемою):** самі тексти статус-сповіщень on-brand — «контролюємо друк», повага до автора, **жодної обіцянки корекції кольору**. Чіпати лише емодзі/оклики/жаргон.

### M-15 · Доки розходяться з кодом (admin «off-brand») — ризик зайвої роботи Claude Code — `P2`
**Де:** `marketing_context/MARKETING_CONTEXT.md` §13 P2 і `WORKLOG.md` (інтегрований бек-лог P2) досі кажуть «уніфікація дизайну `admin` (досі off-brand)». Код (`admin.html`) — **уже на бренді** (іт.2, M-10).
**Проблема:** застарілий бек-лог у джерелі істини → майбутня сесія / Claude Code може **переробити вже зроблене** або довіритись хибному статусу.
**Чому важливо:** ти передаєш інструкції в Claude Code; розбіжність доків і коду коштує часу й довіри до доків.
**Інструкція:** звірити §13 P2 конституції та бек-лог WORKLOG з фактичним станом і **актуалізувати**: позначити admin-уніфікацію зробленою (або точно описати, що саме лишилось — напр. лише M-10 шаблони). Перевіряти й інші P2-пункти на «вже зроблено».
**Не робити:** не редагувати конституцію без відома власника — це його документ; запропонувати правку, не вносити самовільно. `[РІШЕННЯ ВЛАСНИКА]`

## 🟡 Ітерація 3 — аналітика воронки, дисципліна кольору, юр-копія

### M-16 · Верх воронки на лендінгу не вимірюється (CTA без `data-track`) — `P2` (висока цінність для рішень)
**Де:** `index.html` — **нуль** атрибутів `data-track` на всій сторінці (перевірено grep). Не тегнуті: геро «Замовити проявлення» (701), «Я фотограф» (702), фінальний CTA (1070–1071), партнерський «Стати співавтором» (900). `track.js` шле `pageview` авто + кліки лише по `[data-track]`.
**Що вимірюється зараз:** `pageview` (усюди) → `format_select` (`nabir.html:122`) → `order_start`/`order_submit` (`order.html`). Тобто видно лише з моменту, коли людина **вже на nabir/order**.
**Проблема:** найважливіша маркетингова метрика — **чи конвертує геро** (скільки відвідувачів лендінгу тиснуть головний CTA проти відскоку) і **чи працює партнерський заклик** — невидима. Власник ухвалює рішення про копірайт/розташування CTA наосліп.
**Інструкція:** додати `data-track` на ключові CTA лендінгу:
- геро/фінал «Замовити проявлення» → `data-track="cta_order"`;
- «Я фотограф» / «Стати співавтором» → `data-track="cta_photographer"`;
- (опц.) кліки секцій-наборів на index → вони ведуть на `/order?pkg=`, можна `data-track="format_select" data-track-meta="<key>"` як на nabir.
Бекенд (`api-track`/`api-analytics`/воронка в адмінці) уже готовий приймати — потрібні лише атрибути.
**Не робити:** не додавати сторонніх аналітик-скриптів (GA/Pixel) — конституція й `privacy.html §10` обіцяють «без рекламних трекерів». Лише власний `pvTrack`.

### M-17 · Зелений «успіх/оплачено» — колір поза брендовою палітрою — `P2` · `ASK`
**Де (клієнт + фотограф бачать):**
- `order.html:581` `.post-email-done` `#5ccb8f`; `:603` `.np-picked` `#5ccb8f`; `:627` `.status-badge.ok` `#5ccb8f` (галочки «успіх/перевірку пройдено» в флоу замовлення).
- `partner.html:83–84` `.pv-badge.paid` `#5ccb8f`; `:92` `.pv-tg-ok-ic` `#5ccb8f` (бейдж «оплачено», «Telegram підключено»).
- `admin.html` — багато (`#6bbf8a`: badge-paid, kpi.green, trend.up, воронка, LTV, status paid…) — внутрішнє, але те саме рішення.
**Проблема:** `tokens.css` **не містить зеленого**. Палітра навмисно аскетична: темна камера / папір / **сепія** (теплий позитив) / **сафлайт** (сигнал-червоний) / срібло. Stock-зелений «success» — це рівно «генералізація премії в типовий сайт», від якої застерігає конституція (11), і пряме порушення guardrail #2 (не додавати кольори поза `tokens.css`).
**Чому важливо:** зелена галочка на преміум-darkroom-бренді читається як звичайний веб-шаблон; б'є по головній відмінності (НЕ як усі).
**Інструкція (узгодити з власником — це палітра):** обрати ОДНЕ й застосувати скрізь:
- **(А, рекомендовано)** позитив/оплачено → **сепія** (`--sepia`) як теплий ствердний акцент; галочки лишити, колір змінити на сепію/папір. Нуль нових токенів.
- **(Б)** якщо власник хоче саме «зелений = оплачено» для скан-зручності — додати **один семантичний токен** у `tokens.css` офіційно (напр. `--ok`), узгоджений із брендом, і вживати лише його. Це зміна конституційного файлу → лише з дозволу.
**Не робити:** не лишати хаотично `#5ccb8f`/`#6bbf8a` інлайнами; не вводити ще й синій/фіолетовий на клієнтських поверхнях (у адмінці для графіків — окреме питання).

### M-18 · `privacy.html §10` трохи переобіцяє «без відстеження окремих осіб» — `P3`
**Де:** `privacy.html:153` — «знеособлена аналітика… без відстеження окремих осіб». `track.js:8–14` присвоює **постійний** анонімний `pv_sid` у localStorage, що зв'язує перегляди одного пристрою в сесію (+ `ref`/`ph=` атрибуція).
**Проблема:** ідентифікатор сесії хоч і анонімний (без PII) і first-party, та все ж «слідкує» за одним пристроєм між сторінками/візитами — формулювання «без відстеження окремих осіб» дещо сильніше за реальність. Дрібниця, але юр-точність важлива на сторінці приватності.
**Інструкція:** уточнити копію §10: напр. «без міжсайтового відстеження та без рекламних трекерів; для базової аналітики використовується **анонімний ідентифікатор сесії**, що зберігається на вашому пристрої й не повʼязаний з вашою особою». Узгодити з реальною поведінкою `track.js`.
**Не робити:** не вигадувати відповідність GDPR/ЗУ, якої немає; це уточнення формулювання, не юридичний висновок.

## 🟡 Ітерація 4 — джерело цін, бренд-нейминг, a11y, адаптив

### M-19 · Публічна сторінка статусу губить поетичні назви наборів — `P2`
**Де:** `functions/_utils.js:318–323` `PRODUCT_NAMES` = плоскі назви («Великий набір (50–80 фото)»). Воно йде в **`api-order-status.js:34`** → `status.html` (`m-product`, рядок 143) — це **бачить клієнт**, який стежить за замовленням за посиланням.
**Проблема:** клієнт обрав «Великий **«Архів»**» і в кабінеті (`account.html`, з `config.js`) бачить саме поетичну назву, а на **публічній сторінці статусу** — суху «Великий набір (50–80 фото)». Бренд будує «набір як **об'єкт бажання**» з іменами «Момент / Спогад / Архів / Малюк» (конст. 7 nabir DoD, голос «спогад, не зображення») — і губить це рівно в момент очікування набору. Та сама сутність зветься по-різному на двох екранах того самого замовлення.
**Чому важливо:** консистентність назви = частина преміальності; розрив дрібно б'є по довірі й по «обряду».
**Інструкція:** привести `PRODUCT_NAMES` до брендових назв, лишивши лічильник для ясності: напр. `large: 'Великий «Архів» (50–80 фото)'`, `small: 'Маленький «Момент» (12 фото)'`, `medium: 'Подвійний «Спогад» (24 фото)'`, `baby: '«Малюк» (12 фото)'`. **KEEP IN SYNC** з `config.js` `packages[].name`. Альтернатива — щоб `api-order-status` віддавав назву з мапи, узгодженої з config.
**Не робити:** не чіпати ціни/`PACKAGES` (вони — окремий мирор, синхронні; див. нижче). Лише людиночитані назви.

### M-20 · Прев'ю завантажених кадрів — порожній `alt` (a11y-мікрокопірайт) — `P3`
**Де:** `order.html:2748,2899` — прев'ю кадрів клієнта мають `alt=""`.
**Проблема:** для користувача скрінрідера завантажені кадри — «нічого». Порожній alt на користувацькому контенті **припустимий** (presentational), але описовий дав би навігацію.
**Інструкція (опц.):** `alt="Ваш кадр ${i+1}"` на прев'ю в сітці/аркуші. Дешево, в голосі («ваш кадр»). Маркетингові/продуктові фото (геро, галерея, набори) alt уже мають — їх НЕ чіпати.
**Не робити:** не «покращувати» alt продуктових фото — вони вже описові.

### 🔎 Перевірки іт.4 без знахідок (зафіксувати, щоб не передивлятись):
- **Ціновий мирор `config.js` ↔ `functions/_utils.js` — СИНХРОННИЙ.** Звірено поелементно: `PACKAGES` (small 400/medium 600/large 1200 cap80/baby 450), `PRINT_PRICE` 15, `RETAIL_ENABLED` false, `FIRST_SERIES` {enabled,10,15}, `COMMISSION_TIERS` (12/15/20 на 0/5/15), `STD_CHAIN`. Розбіжностей нема — клієнт не побачить ціну, відмінну від серверної. (Єдина незбіжність — людиночитані назви, це M-19.)
- **`favicon.svg` — на бренді:** монограма-діафрагма (темна камера `#1A1817` + паперове коло `#F3EEE6` + сепія `#C4956C` + паперова цятка) точними брендовими hex.
- **`cabinet.css` адаптив** — медіа-брейки на 980px, мобільна нижня панель у зоні пальця, короткі mono-лейбли; явних фікс-ширин >400px не знайдено.

## 🟡 Ітерація 6 — live-рендер мобільних (320/360/375px) + готовий копірайт

> Метод: оскільки вікно Chrome не зменшується нижче desktop-мінімуму, а скріншоти таймаутили на анімаціях, я міряв overflow **усередині iframe** фіксованої ширини на живому `proyav.pages.dev` (медіа-запити в iframe реагують на його ширину). Це точніше за «око»: повертає `scrollWidth` і конкретні елементи-винуватці.

### Результати (живий сайт):
- **Чисті на 320px (немає горизонт. overflow):** `/` (index), `/nabir`, `/order`, `/status`, `/oferta`, `/partnerstvo`, `/partner` (екран входу). Конверсійно-критичні сторінки тримають навіть 320px. ✅
- **Знайдено 2 реальні overflow** (нижче).

### M-21 · `privacy.html` — таблиця обробників даних виходить за екран на ≤320px — `P3`
**Де:** `privacy.html` `.lg-table` (розділ 6 «Кому передаємо дані»), стилі в `legal.css:50–54`.
**Заміряно (live):** 320px → `scrollWidth` 324 (overflow ~4px, реальний горизонт. скрол, бо `body.legal` **не має** `overflow-x:clip`). 360px → вміщається (345). 375px → ОК (360). Тобто страждають лише найвужчі екрани (iPhone SE 1-го пок. 320, старі Android).
**Інструкція:** зробити `.lg-table` адаптивною: обгорнути в `.lg-table-wrap{overflow-x:auto}` АБО на `@media(max-width:360px)` зменшити `padding`/`font-size` клітинок, АБО стекати рядки в картки. Найпростіше — горизонтальний скрол-врапер лише для таблиці.
**Не робити:** не чіпати інші юр-сторінки — `/oferta`, `/partnerstvo` чисті (немає таблиць).

### M-22 · `account.html` — блок `.pv-author` ширший за екран на всіх мобільних — `P3` (косметика)
**Де:** `account.html` `.pv-author` (аватар + ім'я автора + online-крапка), `cabinet.css`.
**Заміряно (live, залогінений кабінет):** елемент фіксовано тягнеться до **391px** незалежно від вьюпорта → overflow 71px@320, 31px@360, **16px@375**. Сторінку рятує `overflow-x:clip` (немає скролу), але блок **обрізається** праворуч на вузьких екранах. Підтверджує нотатку WORKLOG («~74px, кандидат на поліш») точними числами — і що воно є навіть на найпоширенішій 375px.
**Інструкція:** дати імені стискатися замість розпирати блок: на `.pv-author-text`/`.pv-author-name` додати `min-width:0` + `overflow:hidden; text-overflow:ellipsis; white-space:nowrap`; перевірити, що `.pv-author` має `max-width:100%` і `min-width:0` у флексі. Тоді довге ім'я («Олександр Кардаш») трункейтиться, а не виходить на 391px.
**Не робити:** не прибирати `overflow-x:clip` (воно й тримає сторінку від скролу) — фікс саме в трункейті дитини.

---

## 📝 Готовий копірайт (іт.6) — paste-ready для Claude Code
> Тексти в голосі бренду (тихо/тепло/точно/з повагою), без %, без «знижка», без окликів. Класи/токени — лише наявні. Перед вставкою — фінальне «так» власника на формулювання.

### ▸ M-02 — Гарантія як названий стовп біля CTA (`index.html`)
**Куди:** у геро-блок (під `.hero-cta-row`, ~після 703) та/або у фінальний CTA (~після 1071). Подати як тиху «печатку», не банер.
**Три довжини (обрати одну):**
- **Мікро (рядок-eyebrow під CTA):** `Гарантія проявлення — відбиток збереже ваш задум, інакше передрукуємо за наш кошт.`
- **Одне речення (рекомендовано):** «**Гарантія проявлення.** Відбиток збереже ваш задум — калібрування, перевірка під світлом, ваш колір недоторканний. Якщо тон не збігся — передрукуємо за наш кошт.»
- **Розгорнуто (картка):**
  - заголовок: `Наша гарантія`
  - тіло: `Ви бачите кадр на екрані — ми робимо так, щоб відбиток зберіг саме його. Контроль друку на каліброваному обладнанні, ваш колір недоторканний. Не збіглося — передрук за наш кошт, без питань.`
**Розмітка-приклад (наявні класи):**
```html
<div class="first-series-card" style="margin-top:24px">
  <div class="fs-eyebrow">Наша гарантія</div>
  <p class="fs-text">Відбиток <strong>збереже ваш задум</strong> — контроль друку, ваш колір недоторканний. Не збіглося — передрукуємо за наш кошт.</p>
</div>
```
**Чому так:** прямо втілює Північну Зорю (конст. 1); знімає ризик покупки в незнайомого бренду; 0 нових токенів.

### ▸ M-03 — Тихий рядок довіри на `nabir.html`
**Куди:** одразу під підзаголовком (`nabir.html` після рядка 83, перед `Готові набори`).
**Базовий рядок (для всіх):**
`Преміальне проявлення на Epson SureLab. Ваш колір недоторканний, передрук — за наш кошт.`
**Розмітка-приклад:**
```html
<p class="sub" style="margin-top:-18px;color:var(--paper-50);font-size:13px">
  Преміальне проявлення на Epson SureLab. Ваш колір недоторканний, передрук — за наш кошт.
</p>
```
**Опційно, коли є `?ph=` (клієнт від фотографа)** — додати теплий рядок через наявний JS, що вже читає `phRef`:
`Вас рекомендує ваш фотограф — проявимо його кадри так, щоб зберегти задум.`
**Не робити:** не додавати ще CTA/кроків — лише 1 рядок, щоб не ламати лаконічність (конст. 11).

### ▸ M-14 — Єдиний голос транзакційних повідомлень (before → after)
**Правило:** прибрати оклики; прибрати `✅`; лишити **єдиний** теплий знак `🤍`; «пінг» → «звістка».
| Файл:рядок | Зараз | Стало |
|---|---|---|
| `api-orders.js:179` (клієнт, оплата) | `Дякуємо! Оплату отримано. Сподіваємось, набір вас потішив 🤍` | `Дякуємо. Оплату отримано — сподіваємось, набір вас потішив. 🤍` |
| `api-orders.js:17` (фотограф) | `…Дякуємо, що рекомендуєте Прояв 🤍` | **лишити як є** (вже без оклику) |
| `tg-webhook.js:60` (фотограф, старт) | `Вітаємо у Прояв! Щоб отримувати сповіщення…` | `Вітаємо у Прояв. Щоб отримувати сповіщення…` |
| `tg-webhook.js:86` (фотограф, підключено) | `✅ Сповіщення підключено, {name}! … отримаєте пінг тут … Дякуємо, що з нами 🤍` | `Сповіщення підключено, {name}. Тепер ви отримаєте звістку тут щоразу, коли ваш клієнт оплатить набір. Дякуємо, що з нами. 🤍` |
| `status.html:119` (клієнт, готово) | `…набір вас потішив 🤍` | **лишити** (один 🤍, без оклику) |
| `partner.html:653` (копія кабінету) | `миттєвий пінг «ваш клієнт замовив…»` | `миттєву звістку «ваш клієнт замовив…»` |
**Не робити:** не вихолощувати — теплота лишається, прибираємо лише крик/жаргон/рознобій емодзі.

## ✅ Перевірено й добре — НЕ чіпати (щоб наступні ітерації не «виправляли» правильне)
- **Кольорова мова** вичищена на 100% (нуль збігів по «тон шкіри/корекц/редагу» в усіх `*.html`). Конституція 8.2 — закрито.
- **EmailJS/особистий email** прибрано з клієнта (8.1). У клієнтському коді секретів немає.
- **Самореєстрація фотографа** + безкоштовний пробний відбиток (8.3) — `partner.html` 127–172.
- **Прямий шлях «без фотографа»** на кроці 1 (8.7) — `order.html:828`.
- **Рядок приватності** на завантаженні (8.6) — `order.html:969`; профіль `account.html:780`.
- **Грейд vs контроль друку** — бездоганно (`partner.html` 168–169; `partnerstvo.html` 5.2).
- **Голос фотографа** «ви не продаєте — завершуєте досвід» (`partner.html:596`).
- **Таймлайн проти тривоги** (`status.html`, `account.html` detail) — знімає біль «що далі».
- **Ціни — єдине джерело** `config.js`; партнерська економіка й рівні рендеряться з конфіга (index, partner, partnerstvo).
- **Юр-anchor-id** латиницею (C1) — закрито в `partnerstvo.html`.
- **nabir** ховає роздріб, бейдж «Найчастіший вибір», ph-passthrough.
- **(іт.2) Серверні статус-сповіщення** клієнту (`api-orders.js:176–179`) on-brand: «контролюємо друк», без обіцянок корекції кольору.
- **(іт.2) Пінг фотографу про оплату** (`api-orders.js:5–22`) — поважний до автора, з комісією й подякою за рекомендацію.
- **(іт.2) `tokens.css`** — чистий, повний, єдине джерело палітри/типографіки; інлайн-`rgba(196,149,108,…)` = `--sepia` з альфою (припустимо, не порушення).
- **(іт.2) `admin.html`** — уже на бренді (tokens.css, темна камера, scanline, монограма, sepia/safelight, рольовий гейтинг).
- **(іт.2) Галерея — реальні продуктові фото** (`assets/products/`), не стокові мокапи.
- **(іт.3) `privacy.html`** — сильна: окремий §5 «Дані дітей» (знімає страх батьків, конст. 2.2), таблиця обробників, cookieless-аналітика збігається з `track.js`.
- **(іт.3) `oferta.html`** — §6.2 захищає авторський колір навіть у договорі; §8–9 передрук/повернення чіткі й законні.
- **(іт.3) `cabinet.css`** — кольори в брендовій сімʼї (теплі темні/папір), без чужих відтінків.
- **(іт.3) `api-retail.js:60`** — відмова роздробу on-brand: «Ми працюємо лише з наборами… Найменший "Момент" — 12 кадрів.»
- **(іт.3) Конфеті на успіху** (`order.html:3305`) — брендові теплі тони, не «свято з кульками».
- **(іт.3) `track.js`** — cookieless, first-party, ніколи не кидає помилок.
- **(іт.4) Ціновий мирор синхронний** — `config.js` ↔ `_utils.js` (ціни/пакети/комісія/перша серія) збігаються; сервер рахує сам, клієнтський total не довіряється.
- **(іт.4) `favicon.svg`** — брендова монограма-діафрагма точними hex палітри.
- **(іт.4) Серверна авторитетність назв** — `PRODUCT_NAMES`/`packagePrice` на сервері (захист від підробки), лишилось узгодити людиночитані назви (M-19).
- **(іт.5) Конверсійний флоу `order.html` кроки 2–5** — копірайт на бренді: «Куди надіслати **конверт**», «Кадри, які **проявляємо**», секція «**АВТОР**», «Перевіримо **разом**», картка підпису; «Орієнтовна вартість» чесна.
- **(іт.5) Авто-перевірка якості кадрів** (`order.html:2008–2034`) — попередження чіткі й нетривожні («Файл занадто малий…», підказка форматів).
- **(іт.5) OG-картка** (`assets/og/og-cover.jpg`) — оксамитовий конверт зі срібним тисненням «ПРОЯВ» + брендова риска; тихо, преміально, читабельно; без позабрендових шрифтів.
- **(іт.5) `legal.css`** — на бренді (tokens), формально-читабельна типографіка, адаптив (2 кол. → 1 на 560px), без чужих кольорів.
- **(іт.5) Репо-гігієна** — `.gitignore` коректно виключає `_reference/`, `Дизайн_кабінет/`, `*.zip`, `товар_фото/` → чернеткові макети й важкі медіа **не деплояться** (немає публічного витоку чорнеток і роздування білда).
- **(іт.6, live) Мобільний рендер 320px** — `index`/`nabir`/`order`/`status`/`oferta`/`partnerstvo`/`partner` без горизонтального overflow на живому сайті. Конверсійний шлях тримає найвужчі екрани.

---

## 🗒️ Журнал ітерацій

### Ітерація 1 — 2026-06-27
Повний прохід по всіх клієнтських поверхнях: `index`, `order`, `nabir`, `partner`, `status`, `account`, `partnerstvo`, `oferta`, `privacy`, `config.js` + звірка з `MARKETING_CONTEXT.md`, `WORKLOG.md`, `AUDIT.md`. Заведено M-01…M-13. Загальний висновок: фундамент сильний (майже весь беклог конституції реально закрито в коді), знахідки — рівня довіри/конверсії на маржі, не «полагодь основи».
**Покрито:** усі публічні HTML-сторінки + конфіг.
**Ще не пройдено глибоко (для наступних ітерацій):** `admin.html` (лише скан клієнт-facing рядків — потрібен повний UX-прохід під уніфікацію бренду); серверні шаблони авто-сповіщень у `functions/*` (текст, що бачить клієнт/фотограф у Telegram); `tokens.css`/`cabinet.css`/`legal.css` на дисципліну токенів; реальні фото-ассети (якість/автентичність галереї); мобільні поверхні 375px по копірайту.

### Ітерація 2 — 2026-06-27
Глибокий прохід по шарах, не покритих іт.1: **серверна/транзакційна копія** (`functions/*.js`), **`admin.html`** (повний), **дисципліна токенів** (`tokens.css`) і **автентичність ассетів** (`assets/products/`).
**Покрито:** усі `functions/*.js` з україномовними рядками (статус-сповіщення, пінг фотографу, tg-webhook-привʼязка, реєстрація, помилки); `admin.html` (head+CSS+структура); `tokens.css`; інвентар `assets/products/`.
**Нове:** M-14 (голос транзакційної копії — емодзі/оклики/«пінг»), M-15 (доки розходяться з кодом про admin).
**Корекції попередніх:** M-01 (продуктові фото реальні — прогалина саме в процес/обличчя/клієнт-кейс), M-09 (поглинуто в M-14), M-10 (admin уже на бренді — лишились лише шаблони сповіщень).
**Підтверджені сильні сторони (іт.2):** серверні сповіщення on-brand; `tokens.css` чистий; `admin.html` на бренді; галерея — реальні фото.
**Ще не пройдено глибоко (для іт.3):** повний прохід `oferta.html`/`privacy.html` по копірайту (читав вибірково — структура й тон ок, але детальний voice-lint не робив); `cabinet.css`/`legal.css` на хардкод-кольори поза токенами; реальна перевірка мобільних 320–375px (лейаут-оверфлоу `.pv-author`, згадано в WORKLOG); `track.js`/аналітика подій (чи міряємо ключові кроки воронки для маркетингових рішень); тексти Google-Sheets/Telegram-пінга, що **будуються в `order.html`/`api-retail.js`** (сам пейлоад замовлення, не лише шаблони).

### Ітерація 3 — 2026-06-27
Прохід по черзі іт.2: детальний voice-lint `oferta.html`/`privacy.html`, дисципліна кольору в `cabinet.css`/`legal.css`/HTML, покриття воронки в `track.js`, текст замовлення в `order.html`/`api-retail.js`.
**Покрито:** `oferta.html` (повністю), `privacy.html` (повністю), `track.js`, інструментація `data-track` по всіх HTML (grep), hex-кольори в `cabinet.css`/`admin.html`/`partner.html`/`order.html`, клієнт-facing рядки `api-retail.js`, owner-Telegram-пейлоад `order.html:3050–3197`.
**Нове:** M-16 (верх воронки не вимірюється), M-17 (зелений поза палітрою на клієнт/фотограф-поверхнях), M-18 (privacy §10 переобіцяє «без відстеження»).
**Підсилено:** M-07 (oferta чесніша за маркетинг про пік-72), M-11 (порожні реквізити на oferta/privacy).
**Підтверджені сильні (іт.3):** privacy/oferta змістовні й на бренді (захист кольору навіть у договорі, §«Дані дітей»); cabinet.css кольоро-дисциплінований; owner-Telegram-пейлоад утилітарний (емодзі тут ок — внутрішнє); retail-off-копія on-brand.
**Ще не пройдено (для іт.4):** `legal.css`/`cabinet.css` повний прохід по типографіці й адаптиву (не лише кольори); реальна перевірка лейаутів 320/360/375px у браузері (оверфлоу `.pv-author` ~74px — WORKLOG; чи є інші); `favicon.svg`/OG-картка візуально; вміст `Дизайн_кабінет/` (макети — чи відповідають коду); звірка цін `config.js` ↔ `functions/_utils.js` (дзеркало — чи синхронні насправді); a11y-копія (alt-тексти, aria-лейбли як мікрокопірайт).

### Ітерація 4 — 2026-06-27
Прохід по черзі іт.3: ціновий мирор `config.js`↔`_utils.js`, a11y-мікрокопірайт (alt/aria), адаптив CSS, `favicon.svg`.
**Покрито:** `functions/_utils.js` (повністю — ціни/комісія/перша серія/PRODUCT_NAMES/JWT/PBKDF2), звірка з `config.js`; usage `PRODUCT_NAMES` по `functions/*`; `alt=""` по всіх HTML; адаптив-хотспоти `cabinet.css`; `favicon.svg`.
**Нове:** M-19 (статус-сторінка губить поетичні назви), M-20 (порожній alt прев'ю кадрів).
**Без знахідок (зафіксовано):** ціновий мирор синхронний; favicon на бренді; cabinet.css адаптив-дисциплінований; продуктові/галерейні alt описові.
**Ще не пройдено (для іт.5):** реальний рендер 320/360/375px у браузері (`.pv-author` overflow ~74px — WORKLOG; чи є інші реальні обрізання); вміст `Дизайн_кабінет/` (макети vs код — чи не розійшлись); OG-картка `assets/og/og-cover.jpg` візуально (чи читабельна, чи на бренді); `legal.css` типографіка; повний текст `order.html` кроки 2–5 (аркуш налаштувань фото, grid-режим — копірайт мікровзаємодій); п590-рядковий JS `admin.html` на предмет ще якихось off-palette/копірайт-дрібниць у графіках.

### Ітерація 5 — 2026-06-27
Прохід по черзі іт.4: мікрокопірайт конверсійного флоу `order.html` (кроки 2–5 + JS-попередження/тости), `legal.css`, OG-картка візуально, репо-гігієна, макети `Дизайн_кабінет/`.
**Покрито:** `order.html` кроки 2–5 (доставка/завантаження/перевірка/успіх) + авто-перевірка якості (`:2008–2034`) + тости/undo; `legal.css` (повністю); `assets/og/og-cover.jpg` (переглянуто як зображення); `.gitignore`; інвентар `Дизайн_кабінет/`.
**Нових actionable-знахідок НЕМАЄ.** Усе перевірене — на бренді: флоу-копірайт, попередження якості, OG-картка, legal.css; reference/макети коректно виключені з деплою.
**Сигнал: статичний аудит дійшов насичення.** За 5 ітерацій покрито всі публічні сторінки, серверні функції, CSS/токени, конфіг-мирор, ассети, аналітику, OG, репо. Знахідки M-01…M-20 стабільні; нові ітерації дають лише підтвердження «добре».
**Що залишилось і потребує НЕ-статичних методів (не зробити читанням коду):**
- реальний рендер 320/360/375px у браузері (`.pv-author` overflow ~74px — WORKLOG) — потрібен запущений сайт;
- юзер-тестування флоу на реальних людях (де насправді відвалюються) — потрібні користувачі;
- A/B головного CTA й гарантії (M-02/M-16) — потрібен трафік;
- візуальна якість реальних фото процесу/майстерні (M-01) — потрібен новий контент від власника.

### 🎯 Рекомендований порядок для Claude Code (синтез)
Найбільший важіль → найменший. Спершу узгодити пункти `ASK` із власником.
1. **M-16** (data-track на CTA лендінгу) — дешево, вмикає вимірювання, від нього залежать рішення по решті.
2. **M-02** (гарантія в названий стовп біля CTA) + **M-03** (рядок довіри на `nabir`) — копірайт, висока цінність, низький ризик.
3. **M-05 / M-14 / M-17** (узгодити «−15%», голос транзакційної копії, зелений поза палітрою) — `ASK`, бо чіпають рішення/палітру.
4. **M-19 / M-07** (брендові назви на статусі; SLA peak-aware) — консистентність, дешево.
5. **M-01 / M-04 / M-11** — потребують контенту/рішень власника (обличчя, risk-reversal, реквізити).
6. Решта (M-06, M-08, M-09, M-10, M-12, M-13, M-15, M-18, M-20) — поліш, у вільному порядку.

### Ітерація 6 — 2026-06-27 — live-рендер + готовий копірайт
Закрито дві позиції, що потребували НЕ-статичних методів.
**Частина 1 — live мобільний рендер** (`proyav.pages.dev` через iframe-вимір на 320/360/375px): конверсійні сторінки чисті до 320px; знайдено M-21 (privacy-таблиця ≤320px) і M-22 (`.pv-author` ≤375px, підтверджує WORKLOG числами). *Прим.: браузер був залогінений у клієнтський кабінет — міряв лише лейаут, персональних даних не чіпав.*
**Частина 2 — готовий копірайт** для M-02 (гарантія-стовп), M-03 (рядок довіри nabir), M-14 (єдиний голос сповіщень) — вставлено вище як paste-ready (з розміткою й before→after).
**Метод-нотатка для майбутніх перевірок адаптиву:** вікно Chrome не зменшується нижче ~desktop-мінімуму, скріншоти таймаутять на анімаціях — надійно працює **iframe фіксованої ширини + вимір `scrollWidth`/`getBoundingClientRect`** (медіа-запити реагують на ширину iframe).
**Що лишилось (потребує людей/контенту, не коду):** юзер-тестування флоу; A/B гарантії та CTA (після M-16); реальні фото майстерні/процесу + обличчя засновника (M-01).

<!-- НАСТУПНА ІТЕРАЦІЯ: додавай нові M-XX нижче і онови зведену таблицю + статуси. Не видаляй закриті — став DONE + дату. -->
