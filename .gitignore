# Local secrets — NEVER commit
.dev.vars
.dev.vars.*
!.dev.vars.example

# Wrangler / Cloudflare
.wrangler/
.mf/

# Node
node_modules/
npm-debug.log*

# Claude Code local settings (keep launch.json shareable, ignore local prefs)
.claude/settings.local.json

# Local archive & reference material (not part of the deployed site)
# Усі zip-архіви ігноруємо (Cloudflare Pages має ліміт 25 МБ на файл)
*.zip
_reference/
_research_text.txt

# Design source & raw product media (heavy; optimized assets go in /assets instead)
Дизайн_кабінет/
товар_фото/
ТЗ_сайт_Прояв_черга-2.md

# OS / editor
.DS_Store
Thumbs.db
