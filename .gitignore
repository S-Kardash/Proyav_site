# Local secrets — NEVER commit
.dev.vars
.dev.vars.*
!.dev.vars.example

# Wrangler / Cloudflare
.wrangler/
.mf/

# Node
node_modules/
npm-debug.log*

# Claude Code local settings (keep launch.json shareable, ignore local prefs)
.claude/settings.local.json

# Local archive & reference material (not part of the deployed site)
Proyav_site.zip
_reference/
_research_text.txt

# OS / editor
.DS_Store
Thumbs.db
