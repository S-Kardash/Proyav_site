# CLAUDE.md — Проєкт «Прояв»

> Claude Code читає цей файл автоматично з кореня проєкту як контекст кожної сесії.

## 🔴 ДЖЕРЕЛО ІСТИНИ (читати перед будь-якою зміною того, що бачить людина)

Маркетингова конституція проєкту — **обов'язкова до виконання**:

@marketing_context/MARKETING_CONTEXT.md

**Перед будь-якою зміною копірайту, UX-флоу, структури сторінки чи позиціонування** — звірся з нею і дій строго за нею. Вона має пріоритет над естетикою та над «як зазвичай роблять лендінги». Якщо зміна суперечить їй — **не роби, познач конфлікт і запитай.**
Технічні рішення (рефактор, перформанс, баги) — роби вільно, але все, що бачить людина, проходить через конституцію.

Найгостріше (деталі — у файлі вище): Північна Зоря = «**Прояв гарантує, що відбиток збереже задум**»; головний клієнт = **фотограф** (B2B2C); **ніколи** не обіцяти фотографу корекцію його кольору («контроль друку», не «редагуємо»); не вигадувати соц-доказ; голос — тихо/тепло/точно/з повагою; токени — лише з `tokens.css`.

## 🏗️ Архітектура (технічний стан)

- **Стек:** статичні HTML + Cloudflare Pages Functions (`functions/*.js`) → **Supabase (Postgres через REST)** + синк Google Sheets + проксі Nova Poshta. Авторизація — JWT (HS256), паролі — PBKDF2. Без білд-кроку.
- **Сторінки:** `index` (лендинг), `order` (флоу замовлення), `nabir` (вибір формату), `partner` (кабінет фотографа), `admin` (власник).
- **Дизайн-токени:** лише `tokens.css` (Темна камера/Папір/Сепія/Сафлайт; Cormorant/Inter/JetBrains Mono). Нових не додавати.
- **Ціни — єдине джерело:** `config.js` (фронт) + дзеркало в `functions/_utils.js` (`PACKAGES`, `PRINT_PRICE`, `packagePrice`). Тримати синхронно.

### Модель замовлення (3 напрямки, узгоджено з власником)
- **Роздріб (à la carte):** 15₴ × відбиток. `/order` без пакета. `source='retail'`.
- **Пакет (самообслуговування):** фікс-ціна (Малий 400 / Середній 600 / Великий 900 / Малюк 450). `/nabir` → `/order?pkg=…`. `source='package'`. Авто-апгрейд std: 10→20→50; «Малюк» — cap 10.
- **Фотограф:** фікс-ціна пакета, magic-link з адмінки/кабінету. `source='photographer'`.
- Кожен напрямок відстежується в адмінці (бейдж + «Звідки замовлення»).

## ✅ Зроблено (історія)
- Критичні баги order.html: вибір міста (делегування), степпер, undo-toast.
- Баг-світ: `api-debug` (закрито авторизацією), `api-partner-login` (прибрано витік хешу), stored-XSS в адмінці (escape), мояке, дубль-скрол, статус заявки без фото.
- Ціновий рушій: `config.js` + серверний авторитет (`api-finalize`/`api-retail` рахують ціну самі), авто-апгрейд, `paid_at`/`shipped_at`.
- `nabir.html` перебудовано на бренд (вибір формату).
- **P0 маркетинг:** EmailJS+особистий email прибрано з клієнта (8.1); кольорова мова → «контроль друку · ваш колір недоторканний» (8.2); фейковий соц-доказ → чесні «ЗРАЗКИ»/задум (8.5).
- **Онбординг фотографа (8.3):** `api-partner-register` (самореєстрація + auto-login), безкоштовний пробний відбиток (`/order?ph=&trial=1`, власник виконує), багаторазове посилання `/order?ph=<id>` + ассет, рівні комісії (12→15→20 за активністю, `config.js`/`_utils.commissionFor`), `partner.html` перебудовано на бренд із преміум-кабінетом.

## 🗺️ Інтегрований бек-лог (marketing-first; повний — Розділ 13 конституції)
- **P0 — ✅ зроблено:** EmailJS/email (8.1) · кольорова мова (8.2) · соц-доказ (8.5) · онбординг фотографа (8.3).
- **P1:** сповіщення фотографу про оплату (8.4) · прозорість комісії в кабінеті ✅ (рівні+комісія по кожному) · багаторазове посилання + ассет ✅ · підпис фотографа на картці (8.8) · рядок приватності (8.6) · явний прямий шлях на кроці 1 (8.7).
- **P2:** адмінка обліку (дашборд/витрати/склад/комісії — бекенд готовий, лишився UI) · уніфікація дизайну `admin` (досі off-brand; `partner`+`nabir` уже на бренді) · прев'ю клієнтського досвіду.

### Відкриті рішення власника
- Комісія фотографу: зараз 12% (Розділ 10 радить розглянути 15–20% або шкалу).
- Соц-доказ: підхід (концепт-зразки / чесність «відкрились 2026» / реальні кейси).

## 🔧 Локальний запуск
`.claude/launch.json` → статичний сервер (python http.server :8123). Прев'ю-сервер для перевірки змін у браузері.
