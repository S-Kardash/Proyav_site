# WORKLOG — Прояв · робочий блокнот

> Живий журнал стану коду й задач. Оновлюється щосесії. Мета: пережити `/clear` без втрати контексту.
> Маркетингова конституція — `marketing_context/MARKETING_CONTEXT.md` (пріоритет над усім, що бачить людина).
> Спілкування з власником — українською.

Останнє оновлення: 2026-06-28

---

## 🚦 START HERE — стан після ПРОД-ТЕСТУ (2026-06-28) · читати ПЕРШИМ після /clear

Після 9 ітерацій аудитів/фіксів власник виконав свою частину й провів **реальний прод-тест**. Нижче — справжній стан, не теоретичний.

### ✅ Дії власника — ВИКОНАНО (більше НЕ висить, не повторювати в звітах)
- **Міграція Supabase застосована** (схему звірено зі справжньою БД): `orders` має `source` CHECK {retail,package,photographer,**direct**}, `status` {new,uploaded,in_progress,sent,paid}, `product_type` {small,medium,large,baby}, + `first_series, paid_at, shipped_at, uploaded_at, client_id, photographer_id, expires_at(now+72h), cost`; `expenses.photographer_id` ✅; таблиці `failed_orders, settings, order_photos, events, client_notifications, audit_log, tasks, clients, inventory(+log)` — усі є. Функції `claim_first_series/release_first_series` — застосовані (settings-таблиця є).
- **TG webhook** зареєстрований із секретом ✅ · **EmailJS-ключі** деактивовані ✅ · **усі env задані** (ADMIN_PASSWORD, JWT_SECRET, TG_WEBHOOK_SECRET, SITE_URL, NOVA_POSHTA_KEY, SUPABASE_*, TG_*, GOOGLE_*) ✅ · код **закомічено** ✅ · **smoke-тест проведено** ✅.
- **Свідомо відкладено ВЛАСНИКОМ (НЕ блокери, НЕ чіпати без його слова):** `config.legal` (taxId/email) і `config.payment.card` — заповнить пізніше (зараз екран успіху/кабінет/статус коректно показують fallback «реквізити надішлемо особисто»); Cloudflare WAF rate-limit — лише платна версія, доступу немає (лишається best-effort per-isolate, прийнятно); `MANAGER_PASSWORD` — не задано (працює лише вхід власника).
- **Індекси orders:** у реальній схемі лише PK(id)+UNIQUE(token). Явних індексів на `status/created_at/photographer_id/client_id` немає — НЕ блокер на старті, додати при зростанні обсягу.

### ✅ Прод-тест — ПРАЦЮЄ вживу (підтверджено власником)
Замовлення напряму → TG-пінг + /admin ✅ · завантаження кадрів у/з адмінки → R2 ✅ · реєстрація фотографа + посилання + приймання замовлень ✅ · зміна статусу ✅ · виплата фотографу ✅ · юр-сторінки ✅.

### ⚠️ Прод-тест — ВИЯВЛЕНІ СЛАБКОСТІ (це і є наступна робота)
1. **Сповіщення «тихі», слабкий зворотний зв'язок** (наскрізно): нові сповіщення легко проґавити (нема бейджа/тосту); виплата фотографу з'являється непомітно; зміна статусу — TG ненадійний.
2. **Фотографи не мають сайтових сповіщень** — лише Telegram, і він погано протестований.
3. **TG-бот конфлікт ролей:** один бот = і адмінські пінги (TG_CHAT_ID), і сповіщення фотографам (їх tg_chat_id). Власник сам підключив для обох → плутанина; «з телеграмом можуть бути проблеми». Треба розділити/перевірити.
4. **R2:** фото в папці не впорядковані (важко знайти потрібне).

---

## 📋 БЕКЛОГ ДЛЯ НОВОЇ СЕСІЇ (пріоритезовано, з деталями для роботи «з холодного старту»)

### 🔴 P0 — Сповіщення та зворотний зв'язок (головна тема)
- **N1. Помітність сповіщень.** Додати видимий індикатор непрочитаних (бейдж на навігації кабінету) + toast при новому + чітке «N нових». Зараз `account.html` має панель (`notifHTML`/`NOTIFS`, mark-read працює), але БЕЗ помітності. `partner.html` — сповіщень нема взагалі (див. N2). `admin.html` — нові замовлення є бейджем, але інше тихо.
- **N2. Сайтові сповіщення фотографу.** Зараз фотограф отримує ТІЛЬКИ TG. Додати сайтову стрічку в кабінет (аналог `client_notifications`): нове замовлення за посиланням / оплата / виплата. Потрібна таблиця (напр. `photographer_notifications`, міграція) або перевикористати з `events`/окремий ендпоінт; UI в `partner.html` (нова секція/панель + бейдж). Бекенд: тригерити в `api-orders` PUT (при paid/статусах) і в `payout` (admin.html → api-expenses; треба окремий сигнал).
- **N3. Telegram-бот: діагностика + розділення.** `notifyPhotographerPaid` (api-orders.js) шле фотографу ЛИШЕ на `paid` (by design). Перевірити: чи приходить пінг на paid (tg_chat_id linking через /start у tg-webhook.js); вирішити, чи слати на інші статуси (sent тощо). Розглянути окремий бот або чіткіше розмежування «адмін-чат vs фотографи». Власник: «ми це змінимо».
- **N4. Виплата — зворотний зв'язок.** При виплаті (admin `payout` → api-expenses) додати сайтове сповіщення фотографу «вам виплачено X₴» (через N2-механізм) + помітний акцент у кабінеті (банер/тост) + (опц.) TG-пінг.

### 🟡 P1 — UX замовлення (нові звіти власника)
- **U1. Назви файлів при скачуванні з адмінки = налаштування друку.** Щоб оператор бачив параметри й не помилявся. `order_photos` має `color/paper/qty` — формувати ім'я типу `ПРЯ-ABC123_01_color_glossy_x2.jpg`. Файли: `admin.html` (скачування з drawer), `api-admin-photos.js` (підписаний URL → можна додати `&name=`), `api-client-photos.js` (віддача з R2 → `Content-Disposition: attachment; filename=...`).
- **U2. Хибний recovery-банер ПІСЛЯ успіху.** Після успішного замовлення повернення на сайт показує банер «незавершене замовлення + збережені налаштування», хоча воно завершене (фото в адмінці). Фікс: `order.html` — `clearDraft()` має надійно чистити чернетку при успіху; перевірити, чому recovery-банер показується (saveDraft при додаванні фото лишає сліди? draft не видаляється? IndexedDB-фото лишаються?). Логіка: `saveDraft/loadDraft/clearDraft/dismissPhotoBanner/recovery-banner`.
- **U3. Дозавантажений в адмінці кадр ігнорує налаштування замовлення.** Адмін-аплоад іде зі СТАНДАРТНИМИ налаштуваннями (матове/колір) без вибору, навіть якщо замовлення все глянцеве. Фікс: `admin.html` upload у drawer → `api-upload` приймає заголовки `X-Color/X-Paper/X-Qty`, але адмінка їх НЕ передає. Дати оператору вибір color/paper/qty при аплоаді (або успадкувати від домінантних налаштувань замовлення).

### 🟠 P1 — Велике, потребує дизайну
- **U5. Повернення до незавершеного замовлення З фото.** Користувач випадково вийшов → має повернутись точно туди з уже завантаженими кадрами. Складність: чернетка (localStorage/IndexedDB) тримає метадані, файли — великі. Варіанти: (а) перед виходом пропонувати реєстрацію + підв'язати замовлення (лише контрольований вихід); (б) тимчасове збереження файлів у R2 під draft-токен + автовидалення, якщо не повернулись (спертись на `orders.expires_at`=72год). Дизайн: draft-токен → R2-префікс → при завершенні лишаємо, при протуханні — lazy/cron-видалення файлів+запису. **Окрема велика задача — спершу спроєктувати.**

### 🟢 P2 — Операційне/поліш
- **R2-сортування** (U6): ключ `orders/{token}/{ts}_{rand}_{name}` — у R2-консолі важко знайти. Розглянути порядковий префікс `orders/{token}/{NN}_...`. Низький: адмінка кадри й так показує впорядковано, R2-консоль вторинна.
- Deferred з попередніх ітерацій: онлайн-еквайринг; email/SMS-провайдер (авто-підтвердження клієнту); справжній one-click reorder; nudge-ланцюг активації фотографа; БД-агрегати замість завантаження всіх замовлень в адмінку; розбиття `order.html` (3478 р.); orphan-реконсиляція кадрів; повний combobox-wiring НП (aria-expanded); aria-busy/focus-trap.

### 🛠 ЯК ПРАЦЮВАТИ (для нової сесії)
- Прочитай ЦЕЙ блок + `CLAUDE.md` + `marketing_context/MARKETING_CONTEXT.md` (конституція — пріоритет над усім, що бачить людина).
- Прев'ю: launch `static` (python :8123); після тесту — `preview_stop`. **Кеш прев'ю агресивний** → cache-bust `?_cb=Date.now()` при навігації.
- Серверні функції локально не протестуєш (треба Supabase/R2/TG) → `node --check` + логіка, фінально тестує власник на проді.
- `rateLimited(request, {key, limit, windowMs})` — єдина спільна сигнатура.
- Спілкування з власником — українською.

---

## 🏗️ ІТЕРАЦІЯ 9 — партнер-facing історія виплат ✅ (2026-06-20)
Закрито останню велику нестиковку партнерства: фотограф бачив «До виплати», але не бачив, що йому ВЖЕ виплатили.
- `api-partner-orders` GET тепер повертає `payouts[]` (записи `expenses` «Комісія фотографу», прив'язані до фотографа через секцію 7 — `amount`+`date`, desc).
- `partner.html`: нова секція **«Виплати · на картку»** в архіві кабінету — список дат і сум; для нових партнерів (без виплат) прихована. CSS `.pv-payout-*`.
- Тепер фотограф бачить повну картину: Зароблено / Очікує / Виплачено / До виплати + **історію виплат** — повна прозорість грошей (карта болей: «чи заплатять»).
- Перевірено: `node --check` OK; прев'ю — секція з виплатами (суми+дати) і приховування при порожньому; 0 помилок консолі.

## 🏗️ ІТЕРАЦІЯ 8 — канали `source` + health-метрика активації ✅ (2026-06-20)
Продуктовий важіль: ясність звітів + actionable-сигнал власнику.
- **Консолідація каналів** (admin дашборд): релейбл `CH` — `Партнер`→**«Через фотографа»**, `Пакет`→**«Напряму»** (прямий клієнт із набором), `retail`→«Роздріб» (вимкнено). Донат тепер `.filter(value>0)` — порожній мертвий «Роздріб» більше не показується. Усуває плутанину «продуктовий режим vs канал».
- **Метрика активації фотографів** (сторінка СПІВАВТОРИ): нова плитка **«Активовані»** = фотографи з ≥1 **оплаченим** замовленням, + у шапці «N/M З ПРОДАЖАМИ». Це здоров'я каналу №1: видно, скільки зареєстрованих реально конвертують, а скільки сплять (напр. 2/8 → 6 не привели жодного замовлення). Рахується з уже завантажених `S.orders`/`S.photographers`, без зайвих запитів.
- Перевірено в прев'ю: активація «1/3 З ПРОДАЖАМИ» + плитка; релейбл каналів (старий «Партнер» зник); empty-filter; `.stats` auto-fit тримає 4 плитки; 0 помилок консолі.
- **Дрібниця помічена:** стрічка активності показує «Роздрібний друк» для замовлень БЕЗ `product_type` (фолбек-лейбл) — реальні пакети мають product_type, тож зачіпає лише legacy/retail; не критично.

## 🏗️ ІТЕРАЦІЯ 7 — a11y auth-форм + сегменти + латентний баг ✅ (2026-06-20)
Завершення доступності клієнтської частини.
- **Auth-форми** `partner.html` (реєстрація+вхід) і `account.html` (вхід+реєстрація): додано `for`/`id` зв'язки label↔input, `aria-required` на обов'язкових, `role="alert"` на `.err`/`.pv-auth-err`.
- **Сегменти** (order.html): `aria-pressed` на кнопках Instagram/Замовлення і НП/Самовивіз (синхронізується в `setIdentType`/`setDelivery`) + `role="group"`+`aria-label` на контейнерах.
- **🐛 Латентний баг виправлено:** `setIdentType` ітерував **усі** `.seg-btn` (включно з доставкою, що не мають `data-type`) → знімав `.active` з кнопок доставки. Заскоплено до `#ident-seg .seg-btn`. Перевірено: `setIdentType('order')` тепер НЕ зачіпає `del-nova`.
- Перевірено в прев'ю: усі label-зв'язки (partner+account), aria-pressed перемикається коректно, role=alert, латентний фікс, 0 помилок консолі.
- **Клієнтський a11y — закрито** для основних форм/флоу. Лишилось дрібне: повний combobox-wiring НП (aria-expanded), aria-busy на сабмітах, focus-trap у лайтбоксі/модалках.

## 🏗️ ІТЕРАЦІЯ 6 — a11y критичної форми замовлення ✅ (2026-06-20)
Доступність конверсійного флоу `order.html` (найважливіша форма сайту).
- **Зв'язки label↔input** — додано `for`/`id` на всі 6 полів кроку 1 (ім'я/телефон/email/місто/відділення/ідентифікація). Раніше labels не асоціювались зі скрінрідерами.
- **`aria-required="true"`** на обов'язкових полях (ім'я/телефон/місто/відділення/ідентифікація/згода); email — без (необов'язковий).
- **`role="alert"`** на всіх 7 `.field-error` — оскільки помилки показуються через встановлення `textContent` (рядки 1709/1767/1931), SR тепер озвучує їх при появі.
- **Керування фокусом** — `goTo()` переносить фокус на заголовок нового кроку (`.h-screen`/`.success-title`, tabindex=-1, preventScroll): SR озвучує крок, клавіатура не зависає на старій кнопці «Продовжити».
- Свідомо НЕ додав `role="combobox"`+`aria-expanded` на місто/відділення — їх треба перемикати JS при відкритті дропдауну, інакше стан брехливий (гірше за відсутність); відкладено до повного wiring.
- Перевірено в прев'ю: усі 6 зв'язків + 6 aria-required (email коректно без) + 7 role=alert; `goTo(2)`→фокус на заголовок; 0 помилок консолі.
- **Відкладено далі:** a11y auth-форм (`partner.html`/`account.html` — той самий патерн label-без-for); `aria-pressed`/radiogroup на сегментах (Instagram/Замовлення, НП/Самовивіз); aria-busy на сабміті; combobox-wiring НП.

## 🏗️ ІТЕРАЦІЯ 5 — консолідація + надійність кадрів ✅ (2026-06-20)
Прибирання боргу, який сам аудит і виявив.
- **🔴 Реальний баг надійності: реєстрація кадру в `api-upload` була fire-and-forget без `waitUntil`** → рантайм міг скасувати promise ПІСЛЯ відповіді → кадр у R2, але невидимий (адмінка/кабінет лістять з `order_photos`, не з R2 — підтверджено в `api-admin-photos`). Тепер вставка `await`+1 ретрай; повертаємо `registered` у відповіді.
- **Консолідація rate-limit (3→1).** Прибрано inline `npLimited` (`api-novaposhta`) і `sendLimited` (`send-order`); усі **9 ендпоінтів** тепер на спільній `rateLimited(request,{key,limit,windowMs})`. Це усуває джерело плутанини сигнатур (через яку був баг Іт.4 у `api-order-contact`).
- **Прибрано мертвий код:** `send-order` фото-шлях (base64→Telegram sendPhoto + R2-архів) — order.html шле лише `type:'text'` (підтверджено: `apiPost` тільки з тексту), кадри йдуть у R2 через `api-upload`. `send-order` 126→54 рядки; менша поверхня атаки (немає base64-декоду у воркері).
- Перевірено: `node --check` 4 функцій OK; усі rate-limit виклики — канонічна сигнатура (grep). Серверне тестується на проді (R2/Supabase).
- **Відкладено далі:** orphan-реконсиляція (бекфіл `order_photos` з R2 за префіксом) — тепер маловажливо після await-фіксу; клієнтський a11y форм; edge-кейси відновлення чернетки.

## 🏗️ ІТЕРАЦІЯ 4 — Аудит 3 (ендпоінти, надійність, квоти) ✅ (2026-06-20)
Прохід по неперевірених ендпоінтах (upload/НП/finalize/send-order/tg-webhook) + надійність.
- **🔴 Баг (мій, з Іт.3): `api-order-contact` rate-limit — no-op.** Викликав `rateLimited('contact:'+ip, 20, 60000)` — стара/неправильна сигнатура; канонічна `rateLimited(request,{key,limit,windowMs})`. `request`=рядок → `.headers.get` кидало → catch повертав `false` → ліміт не працював. Виправлено на канонічну сигнатуру.
- **Надійність кадрів:** цикл завантаження в R2 (`order.html submitOrder`) тепер **ретраїть 1 раз** (800ms) при збої — кадри це головний результат, мережеві обриви більше не гублять їх одразу.
- **Квота Нової Пошти:** `api-novaposhta` віддає `Cache-Control: public, max-age=3600` на міста/відділення → Cloudflare/браузер кешують, автокомпліт не б'є по квоті ключа на кожне натискання.
- **Безпека:** `tg-webhook` ротує `tg_link_token` після привʼязки (витекле посилання не перепривʼяже сповіщення); `api-finalize` отримав rate-limit (публічний POST).
- **LAUNCH_CHECKLIST:** env-таблиця оновлена — `SITE_URL`/`TG_WEBHOOK_SECRET`/`ADMIN_PASSWORD`/`JWT_SECRET`/`NOVA_POSHTA_KEY`/Google задані (підтверджено власником); лишилось не-env: міграція секцій 6/7/8, EmailJS-деактивація, config.legal/payment, WAF, індекси.
- **Знайдено, відкладено:** 3 різні реалізації rate-limit (shared `rateLimited` + inline `npLimited`/`sendLimited`) → консолідувати; `send-order` фото-шлях (base64→TG) мертвий (order.html шле лише text; кадри в R2) → прибрати; orphan-кадр якщо `order_photos`-insert впав (файл у R2, але не в реєстрі).
- Перевірено: `node --check` 5 функцій OK; order.html парситься, 0 помилок консолі.

## 🏗️ ІТЕРАЦІЯ 3 — Аудит 2 (код-рівень, нестиковки) ✅ (2026-06-20)
Глибший прохід після Ітерації 2. Головний кластер — економіка партнерства не стикувалась між кабінетом / пінгом / адмінкою.
- **Блок A — уніфікація економіки партнерства (2A-1, 2B-3).** Було: кабінет фотографа рахував комісію з УСІХ замовлень × tier-pct, адмінка платить лише за `paid` × `commission_pct` → фотограф бачив більше, ніж отримає. Стало **один тариф = збережений `commission_pct`** (його ж платить адмінка) + база **лише `paid`**. Кабінет показує «Зароблено / Очікує оплати / Виплачено / **До виплати** (net)». Рівень росте з **оплаченими**; `api-orders` авто-піднімає `commission_pct` коли фотограф перетинає поріг тарифу (реалізує авто-зростання конституції, єдине джерело). `notifyPhotographerPaid` бере pct прямо (прибрано зайвий запит 2C-8). Картка комісії на home — лише для оплачених (2C-9).
- **Блок B — `SITE_URL` (2A-2, launch-блокер, який я проґавив).** Посилання клієнту (`api-orders`/`api-partner-orders`/`api-partner-register`) будувались із `env.SITE_URL`; якщо не задано → відносне `/order?t=` (зламане при пересиланні). Новий `siteOrigin(env,request)` у `_utils` — фолбек на origin запиту (працює й на кастомному домені). + `SITE_URL` у LAUNCH_CHECKLIST.
- **Блок C — дедуп сповіщень + валідація статусу (2B-4, 2B-7).** `api-orders` PUT читає поточний статус ДО оновлення → нотифікує/штампує `paid_at`/`shipped_at` лише при РЕАЛЬНІЙ зміні (пересейв уже-оплаченого більше не дублює сповіщення клієнту/фотографу). Статус валідується проти `FLOW`.
- **Блок D — «як оплатити» + email (2B-5, 2B-6).** Кабінет клієнта (`account.html` `payInfoHTML`) і `status.html` тепер показують суму+картку (з `config.payment`) для НЕоплачених замовлень. Захоплений email тепер зберігається в нотатки замовлення (новий `api-order-contact.js`, token-gated), а не лише в Telegram.
- Перевірено: `node --check` 5 функцій OK; прев'ю — кабінет (рейт/зароблено/net), `payInfoHTML` (4 кейси), status pay-card (показ/прихов), 0 помилок консолі. ⚠️ Власнику: `SITE_URL` в env (бажано); решта міграцій без змін.

## 🏗️ ІТЕРАЦІЯ 2 — PM-аудит: launch-блокери + реальні баги ✅ (2026-06-20)
Після детального архітектурного аудиту (customer journey, повнота, операційна готовність). Виправлено код-блокери; великі рефактори (онлайн-еквайринг, розбиття order.html, БД-агрегати) — наступні ітерації.
- **Блок A — облік виплат фотографам (реальний баг із грошима).** Було: `phCommission.owed` = ВСЯ нарахована комісія, `payout` лише писав витрату → борг не зменшувався → **ризик подвійної виплати**. Стало: `owed` = чистий (нараховано − виплачено). Виплата лінкується до фотографа (`expenses.photographer_id`, **секція 7 міграції**); `api-expenses` приймає його з graceful-фолбеком (працює і до міграції). Сторінка партнерів: статистика «Нараховано/Виплачено/До виплати» + колонка net + перерендер після виплати. Перевірено: accrued 120, виплачено 60 → owed **60**, не 120.
- **Блок B — платіжна ясність (journey 2.3/7.1).** Новий `config.payment` (картка/holder/cod, керує власник). Екран успіху `order.html` тепер показує **конкретну суму + картку** для переказу (`renderPaymentInfo`), якщо власник заповнив картку; інакше — поточний текст; пробний → «безкоштовно». Знімає провал «клієнт не знає скільки і куди платити».
- **Блок C — сейфнет «впалих» замовлень (B8).** Якщо вставка в `orders` остаточно провалилась → сирий payload іде в нову таблицю `failed_orders` (**секція 8 міграції**) + `api-retail` пише туди (best-effort). Новий `api-failed-orders.js` (staff GET/resolve). Адмінка: завантаження на boot + **червоний алерт на дашборді** + модалка зі списком і «Опрацьовано». Більше не залежимо лише від Telegram-пінга.
- **Блок D — `LAUNCH_CHECKLIST.md`** — зведені дії власника (міграція з секціями 6/7/8, env, webhook, EmailJS, config.legal+payment, WAF, індекси) + smoke-test перед запуском.
- Перевірено: `node --check` 5 функцій OK; прев'ю — phCommission net, алерт+модалка «впалих», renderPaymentInfo (3 гілки), 0 помилок консолі. ⚠️ **Власнику: міграція тепер має секції 6, 7, 8** (див. LAUNCH_CHECKLIST).
- **Знайдено в аудиті, відкладено (не код / наступні ітерації):** онлайн-оплата (еквайринг); автопідтвердження email/SMS клієнту (немає провайдера); справжній reorder; воронка активації фотографа + nudge-ланцюги; БД-агрегати замість `S.orders`-усе в адмінці; розбиття `order.html` (3478 р.); консолідація send-order/retail; партнер-facing історія виплат.

## 🎨 DESIGN-CRITIQUE ФІКСИ ✅ (2026-06-20) — після критики всього сайту
Прогнав `/design-critique` по всіх публічних поверхнях. Виправлено реальні проблеми; частину «знахідок» свідомо лишено (конституція має пріоритет над загальними UX-евристиками).
- **🔴 Прямий шлях на order крок 1 (8.7) — РЕАЛЬНО полагоджено.** Було позначено ✅, але `showPackageBanner` **завжди** ховав `#direct-note`, а оскільки роздріб-off → кожне замовлення має пакет → прямий клієнт (без фотографа) нотатку «замовляєте напряму» НІКОЛИ не бачив. Тепер `toggle('hidden', !!S.photographerId)` — ховаємо лише для клієнтів фотографа; копію нотатки переписано під нового прямого клієнта («впишіть свій Instagram для зв'язку…»). Перевірено: без `?ph` — видима, з `?ph` — прихована.
- **nabir підзаголовок** — прибрано згадку вимкненого «друку поштучно» → «Готовий набір у преміальному пакуванні. Далі ви завантажите свої кадри.»
- **Тач-таргети** — `.pv-btn--mono` 40→44px (cabinet.css), inline-кнопка «Поділитися» 40→44px.
- **Свідомо НЕ чіпав (за конституцією):** (1) 9px моно-мікролейбли — це **навмисний site-wide брендовий патерн** (~40 місць), проходить WCAG AA (paper-50 ≈ 4.7:1); інфляція = «генералізація премії в типовий сайт» (заборонено Розд. 11). (2) Мобільний топ-нав index = одна-CTA — легітимний преміум-патерн, усе досяжне скролом + футером. (3) Safelight(лендинг)/сепія(кабінети) для CTA — принциповий поділ «маркетинг=сигнал, інструмент=акцент», репейнт нашкодив би спокою кабінету.

## 🔬 CODE-REVIEW ФІКСИ ✅ (2026-06-20) — після xhigh-рев'ю цієї сесії
Прогнав `/code-review` (xhigh, 10 кутів через агентів). Promise-логіка модалок і SQL-атомарність — підтверджено коректними. Виправлено всі знахідки:
- **order.html (C2):** бейдж знижки хардкодив «15%» і вживав «знижк**у**» (конст. Розд. 5 забороняє) → тепер «**Застосовано ціну «першої серії»**» (без %, без «знижка», подано як ціновий рівень). `renderSuccessTotal` отримав null-guard (`valEl`/`discEl`). `total_amount`+`first_series` тепер читаються **разом** (бейдж не зʼявиться біля незниженої суми). Magic-link шлях тепер бере server-authoritative суму з відповіді `api-finalize` (`finalizeWithToken` повертає JSON, await на magic-гілці).
- **B5 фантомний слот:** `claimFirstSeries` бронює слот ДО вставки; якщо вставка остаточно провалилась (`firstSeries && !order`) — `releaseFirstSeries()` повертає слот. Нова RPC **`release_first_series`** (decrement, floor 0) у секції 6 міграції. Сід секції 6 загорнуто в `DO ... exception when undefined_column/table` — не валить міграцію при запуску секції окремо.
- **Прибирання:** зайвий `.catch(()=>false)` на `claimFirstSeries` (ніколи не кидає); мертвий `if(!o.client_id)return` в `api-clients` (надлишковий через фільтр); `_modalDismiss` в адмінці — `modal()` тепер резолвить незавершений confirm/prompt як «скасовано» (hardening проти майбутніх вкладених модалок).
- Перевірено: `node --check` 5 функцій OK; прев'ю — renderSuccessTotal (3 кейси + null-guard), модалки (confirm/prompt/backdrop + вкладена-не-зависає), 0 помилок консолі. ⚠️ Власнику: секція 6 міграції тепер містить і `release_first_series`.

## ⚙️ МАСШТАБ-ЗМІЦНЕННЯ B1 + B5 ✅ (2026-06-20) — аудит
Превентивне зміцнення під потік (власник обрав зробити зараз). **Весь код-беклог аудиту тепер закрито.**
- **B1 (агрегати без `limit:9999`):** `api-photographers`/`api-clients` більше не тягнуть усю таблицю orders у воркер — додано `filters: { photographer_id/client_id: 'not.is.null' }`, тобто агрегуємо лише атрибутовані замовлення (релевантну підмножину; нерелевантні й так пропускались у циклі → результат ідентичний, payload набагато менший). Без міграції, нуль регресу.
- **B5 (гонка лічильника «Першої серії»):** count-перед-вставкою міг перевищити ліміт при одночасних замовленнях. Тепер атомарно: **міграція секція 6** — `settings.first_series_claimed` (сід із наявних позначених замовлень) + RPC `claim_first_series(p_slots)` (один `UPDATE…WHERE value<slots RETURNING` серіалізує конкурентні заявки). Додано `db.rpc()` у `_utils`; `claimFirstSeries()` замінив `firstSeriesUsed`-гейт в `api-retail`; `firstSeriesUsed` (і публічний `api-first-series`) тепер читають той самий лічильник → гейт і дисплей з одного джерела. **Фолбек:** якщо RPC ще немає (міграцію не застосовано) — старий count-метод (нуль регресу).
- Перевірено: `node --check` усіх 5 функцій OK, імпорти/експорти узгоджені. ⚠️ **Власнику: застосувати секцію 6** `migrations/proyav_admin.sql` (до того атомарності немає, але працює старий метод). Серверну логіку тестувати на проді (потребує Supabase).

## 🪟 МОДАЛКИ В АДМІНЦІ ✅ (2026-06-20) — аудит C6
Прибрано нативні `confirm()/prompt()` з `admin.html` (8 місць) → promise-based `confirmModal(msg,opts)` / `promptModal(opts)` у стилі панелі (реюз `.modal/.modal-head/.action/.action.ghost/.action.danger/.input`). Закриття по бекдропу або ✕ резолвиться як «скасовано» через `_modalDismiss`, який підхоплює `closeModal()` (інші модалки його не ставлять → no-op). `adjustStockN` зроблено `async`; решта call-site'ів уже були async. Перевірено в прев'ю: 0 `confirm/prompt` лишилось, обидва хелпери визначені, 0 помилок консолі; promise-логіка — yes→true / cancel→false / бекдроп→false / prompt→значення / cancel→null. (Скриншот цієї сторінки зависає через важкий scanline-рендер — не пов'язано зі зміною; перевірено функціонально.) `alert()` уже не було (паролі — `credModal`).

## 💴 ЧЕСНА СУМА НА ЕКРАНІ УСПІХУ ✅ (2026-06-20) — аудит C2
Раніше `order.html` ніде не показував реальну суму: огляд = «Орієнтовна вартість» (повна), сервер тихо знімав −15% «першої серії» → клієнт не бачив, що заплатить. Тепер екран успіху показує **server-authoritative** число.
- **Сервер** (`api-retail`): відповідь додатково повертає `first_series` + `discount_pct` (`total_amount` уже зі знижкою повертався).
- **Клієнт** (`order.html`): `submitOrder` ловить `res.total_amount`/`res.first_series`; новий блок «СУМА ЗАМОВЛЕННЯ» поряд із номером (`renderSuccessTotal`) + тиха сепія-мітка «Перша серія · знижку 15% враховано». Free-trial → «Безкоштовно». Якщо сервер не відповів — фолбек на клієнтську оцінку. Magic-link (`api-finalize`) знижки не застосовує → там розбіжності й не було.
- Бокси НОМЕР+СУМА загорнуто в `.success-meta` (flex-wrap) — рівне вирівнювання, без overflow. Перевірено в прев'ю: 3 кейси (знижка/без/безкоштовно), desktop+mobile 375, 0 помилок.

## 🔗 OPENGRAPH / СОЦ-ПРЕВ'Ю ✅ (2026-06-20) — аудит C5
Додано `og:*` + `twitter:card` (`summary_large_image`) + meta description на **index / nabir / order / partner**. Копірайт за конституцією (North Star «щоб відбиток зберіг задум»; «контроль друку · колір недоторканний»; без знижок/соц-доказу; partner — голос автора). Спільна **OG-картка** `assets/og/og-cover.jpg` (1200×630, 98 КБ) — згенерована PIL з геро-фото `envelopes-01` (оксамитовий конверт із тисненим «ПРОЯВ» + брендова риска, без доданого тексту → жодних позабрендових шрифтів). Тепер шеринг `nabir?ph=` фотографом у Telegram/Viber дає преміальне прев'ю (раніше — голе посилання). Перевірено: усі 4 сторінки парсяться, картка віддається 200/jpeg. ⚠️ Домен `proyav.pages.dev` зашито абсолютно в `og:url`/`og:image` — за зміни домену find/replace у 4 файлах (нотатка в коментарі кожного блоку).

## 🎒 ПАРТНЕРСЬКИЙ «КІТ» + ПРЕВ'Ю КЛІЄНТА ✅ (2026-06-20) — маркетинг P2 #12/#11
Нова сторінка **«Матеріали»** в кабінеті фотографа (`partner.html` `renderKit`, 5-й пункт навігації, icon-стопка). Закриває маркетингову конституцію Розділ 13 P2 #11 і #12 (усі P0/P1 уже були закриті). Усе через конституцію: голос куратора, фотограф=автор, «колір недоторканний», без продажу/знижок/соц-доказу, лише наявні класи `cabinet.css`/`tokens.css`.
- **Готові рядки** — 5 копі-пейст ассетів нашим голосом (щоб фотограф не підбирав слова й не звучав як продавець): футер галереї · Instagram-підпис · текст для сторіс · **відповідь у директ «а де надрукувати?»** (вирішує точний «продажний» момент: «друкують мій файл точно як є») · короткий підпис під постом. Усі ведуть на `nabir?ph=<id>`.
- **Гайд «без відчуття продажу»** (#12) — рамка-callout «Ви не продаєте. Ви завершуєте досвід» + 5 принципів (один раз спокійно / про результат не покупку / ім'я поруч із кадром не з ціною / розмову беремо ми / тиша — теж відповідь). Знімає головний психологічний бар'єр фотографа (Розд. 2.1).
- **Прев'ю «Що бачить ваш клієнт»** (#11) — реальний 4-кроковий шлях на `.pv-route` (набори→4 кроки→сповіщення→набір у руках) + гарантія «розмову й передрук беремо ми · колір недоторканний» + кнопка «Відкрити як клієнт ↗» (живе `nabir?ph=`, не мокап — знімає страх «винним буду я»).
- Крос-лінк зі сторінки «Проявити» → «Матеріали». Новий CSS — лише `.kit-*` (без нових токенів).
- **Перевірено** в прев'ю (desktop+mobile 375): рендер ок, 0 помилок консолі, URL переносяться в сніпеті. ⚠️ Передіснуюче (не від цього блоку): шапка кабінету `.pv-author` дає ~74px overflow при довгому імені автора — ховається `body.pv{overflow-x:clip}`, візуально безпечно; кандидат на поліш окремо.

---

## 📋 ТЗ ЧЕРГА 2 (12.06.2026) — статус виконання
Джерело: `ТЗ_сайт_Прояв_черга-2.md` (у .gitignore). Усі дані Розділу 1 заповнені власником.
- **П0.1 Деплой/безпека** ✅ — прод (proyav.pages.dev) чистий від секретів (grep репо + curl прода = 0); гілка `fix/order-hot-bugs` видалена; `main` синхронний з origin. ⚠️ Власнику: **деактивувати старі ключі EmailJS** в акаунті (були публічні).
- **П0.2 Роздріб OFF** ✅ — `config.retailEnabled=false` + `_utils.RETAIL_ENABLED`; nabir ховає роздрібну картку; order.html без набору → redirect `/nabir`; api-retail м'яко відмовляє (422) роздробу (free-trial живе); FAQ-міст на index. Повернення = 1 флаг.
- **П0.3 Архів 1200** ✅ — config+_utils `large.price=1200` (cap 80, без доплат); оновлено index-картку, account SETS (тепер з config, дубль прибрано), admin (PRODUCT_LABEL без цін). Авто-апгрейд 400→600→1200 ✓.
- **П0.4 Контакти** ✅ — футер index (Instagram proyav_ua / t.me/copylounge / tel:+380737679757); «напишіть» на nabir/status → Telegram; order крок1 «є питання — напишіть» (Telegram). 0 `href="#"` у контактах.
- **П0.5 Економіка партнера** ✅ — блок «Скільки це в грошах» на partner.html (до входу) і в партнерській секції index — **рендер з config.js** (48/60/80 · 72/90/120 · 144/180/240); рівні; гарантія передруку; FAQ партнера (4 питання).
- **П1.1 «Перша серія»** ✅ — `config.firstSeries`+`_utils.FIRST_SERIES`+`firstSeriesUsed()`; знижку −15% рахує **сервер** (api-retail), мітка `first_series` (міграція секція 8) + у Telegram-пінгу; публічний `api-first-series` → лічильник місць; тиха секція на index (зникає коли слотів 0/enabled:false).
- **П1.2 SLA** ✅ — `config.sla{48/72/peak}`; рядок на кроці 1 і на успіху order.html (peak-aware).
- **П1.3 Приватність** ✅ — чесний текст (R2-архів кабінету, не «видаляємо»).
- **П1.4 Прямий шлях** ✅ — був на місці (direct-note крок 1).
- **П1.5 Мобільний зум** ✅ — прибрано maximum-scale/user-scalable з order.html (інші не мали).
- **П1.6 Порівняння + «Найчастіший вибір»** ✅ — nabir: бейдж на medium + рядок «для кого» (`audience` у config).
- **П1.7 Роздільність** ✅ — поріг 1200×1772; м'який текст; мітка LOW-RES у Telegram-капшні.
- **П1.8 Оплата** ✅ — рядок на екрані успіху (накладений платіж / картка, реквізити особисто).
- **П1.9 Підпис фотографа UI** ✅ — поле «Підпис на картці» в partner.html + `card_signature` в api-partner-orders → notes.
- **⏳ Лишилось власнику:** виконати **повну міграцію** в Supabase (нова секція 8 `first_series`; + 1–7 якщо ще ні). Деактивувати ключі EmailJS.
- **П2 (не починати поки):** Cloudflare Web Analytics; OpenGraph; email-провайдер; апсейли.

---

## ⚖️ ЮРИДИЧНА ЧАСТИНА ✅ (2026-06-12)
3 юр-сторінки на бренді (legal.css), реквізити з одного джерела `config.legal`:
- **privacy.html** — Політика конфіденційності (12 розділів): точно за реальними потоками даних; таблиця процесорів (Нова Пошта, Cloudflare, Supabase, Telegram, Google); окремі розділи про кадри й дані дітей; права суб'єкта (ЗУ «Про захист персональних даних»); безпека (PBKDF2/JWT/HTTPS/підписані URL).
- **oferta.html** — Публічна оферта (13 розділів): замовлення/оплата (накладений/картка, без онлайн-оплат)/доставка НП/гарантія передруку/повернення (персоналізований товар не підлягає за ЗУ «Про захист прав споживачів», але дефект → передрук)/права на кадри/відповідальність/форс-мажор.
- **partnerstvo.html** — Умови партнерства (8 розділів): комісія (рівні з config), виплати, **дані клієнтів** (партнер гарантує згоду), авторські права + «колір недоторканний», обов'язки, припинення.
- **Згода-чекбокси** (блокують дію): order.html (перед «Передати в роботу» + право на кадри), partner.html (реєстрація), account.html (реєстрація), order.html нудж реєстрації.
- **Футер-посилання** на юр-сторінки: index, nabir, order, status.
- ⚠️ **ВЛАСНИКУ ЗАПОВНИТИ** в `config.js` → `legal`: `entity` (ФОП ПІБ), `taxId` (РНОКПП), `reg`, `address`, `email` (окремий робочий). Рекомендовано показати юристу перед запуском.

## 🔍 ГЛИБОКИЙ АУДИТ (2026-06-12) → див. **`AUDIT.md`**
Проведено повний аудит безпеки/масштабу/дефектів. Головне (деталі+фікси в AUDIT.md):
- 🔴 **Немає rate-limiting** (брутфорс admin/partner/client логінів, абʼюз novaposhta/send-order/retail). Адмін — один спільний пароль = майстер-ключ.
- 🔴 **tg-webhook secret опційний** → зробити обов'язковим.
- 🟡 **limit:9999** вантажить усі замовлення у воркер (api-orders/photographers/clients) — рахувати на БД.
- 🟡 **Google Sheets синк блокує** відповідь замовлення (`await`) → `waitUntil`.
- 🟡 Перевірити індекси `orders.token/status/created_at`.
- 🟢 Дрібниці: anchor-id у partnerstvo, favicon, .dev.vars.example, OpenGraph, alert/confirm у адмінці.
- ✅ Добре: auth скрізь, секретів у клієнті 0, PBKDF2, серверні ціни, R2-підписані URL, захист від втрати замовлень.

## 🎯 НАСТУПНІ ВЕЛИКІ ЗАДАЧІ (деталі в AUDIT.md)
1. ✅ **Сховище файлів** — ЗРОБЛЕНО: `api-upload.js` (бінарний R2-аплоад, оригінал-якість), order.html вантажить кадри в R2 (не Telegram), admin drawer — завантаження/скачування. Telegram = текст-пінг. (Лишилось опц.: ZIP усього замовлення, presigned-PUT.) **Протестувати на проді** (R2 binding потрібен).
2. **Адмінка → CRM** ← *в роботі, велику частину зроблено 2026-06-12:*
   - ✅ **First-party аналітика (як FB pixel, без кук)**: `events` таблиця + `api-track.js` (публічний прийом подій) + `track.js` (трекер на index/nabir/order/partner: pageview + кліки `[data-track]` + order_start/order_submit/format_select) + `api-analytics.js` (агрегати) + **вкладка «Аналітика»** в адмінці (відвідувачі/перегляди/конверсія, графік по днях, джерела, воронка, топ-сторінки).
   - ✅ **Сповіщення клієнтам**: `client_notifications` + `api-notifications.js`; авто-сповіщення при зміні статусу замовлення (api-orders PUT); ручні з адмінки (картка клієнта); **панель сповіщень у кабінеті** account.html (з лічильником непрочитаних + «прочитано»).
   - ✅ **Картка клієнта (CRM)**: `api-clients` детальний вид (історія/події/нотатки/LTV); drawer в адмінці — профіль, теги, нотатки, історія замовлень, активність (events), надсилання сповіщення, **скидання пароля клієнту** (`api-client-auth` action `admin_reset`).
   - ✅ **Журнал дій (audit log)**: таблиця `audit_log` (міграція секція 10) + хелпер `logAudit()` у _utils + запис у api-orders(статус/редаг.)/api-expenses(виплати)/api-photographers/api-clients/api-client-auth(reset)/api-notifications; `api-audit.js` + **вкладка «Журнал»** в адмінці (фільтри по типу дії).
   - ✅ **Масові дії** в замовленнях: чекбокси (+ select-all), панель «N обрано» → «Просунути статус» / «Експорт обраних» / «Скинути».
   - ✅ **Завдання/нагадування**: `tasks` (міграція секція 11) + `api-tasks.js` + вкладка «Завдання» (форма, пріоритет, термін, done/del) + **дашборд-віджет** + бейдж прострочених у навігації.
   - ✅ **Глобальний пошук** у сайдбарі: замовлення + клієнти + партнери → клік відкриває (drawer/картку).
   - ✅ **ZIP-скачування** всіх фото замовлення (власна store-zip, без бібліотек; кнопка «↓ ZIP усіх» у drawer).
   - ✅ **UX для оператора/підтримки (2026-06-12)** — аналіз за ролями виявив прогалини, виправлено:
     - картка замовлення: на кожному кадрі видно **налаштування друку** (Кол/Ч-Б · Мат/Глянець · ×N) + **зведення «Друкувати: …»** (оператору);
     - швидкі дії клієнта: **☏ Подзвонити (tel:)**, копі номер, ↗ Instagram, **«Картка →»** (перехід у картку клієнта);
     - **Доставка** парситься з нотаток і показується окремим полем;
     - паролі — копійована **credModal** замість alert.
   - ✅ **Ролі owner/manager + закрито авторизаційну діру (2026-06-20):** під час побудови ролей виявлено, що `api-orders`/`api-admin-photos` та інші staff-ендпоінти перевіряли лише `authRequest` (будь-який валідний JWT, включно з клієнтським `role:'client'`) — теоретичний доступ клієнта до адмін-даних. Додано `isStaff`/`isOwner` у `_utils` і гейтинг **усіх** адмін-ендпоінтів: **staff (admin+manager):** api-orders, api-admin-photos, api-clients, api-tasks, api-notifications(POST), api-inventory, api-upload; **owner-only (admin):** api-photographers, api-expenses, api-settings, api-debug, api-analytics, api-audit. `api-admin-login` приймає `MANAGER_PASSWORD` → роль `manager`, повертає `{token,role}`. **admin.html:** зберігає роль, `NAV.filter(allowed)` ховає від менеджера гроші/аналітику/партнерів/журнал/налаштування (бачить лише Замовлення/Матеріали/Клієнти/Завдання), лендінг менеджера = Замовлення, комісія партнера прихована в картці замовлення, підпис ролі під брендом. Перевірено: client-JWT (`role:'client'`) відхиляється `isStaff`/`isOwner`; node --check усіх 13 функцій + парс admin.html JS — OK.
   - ⏳ Лишилось: решта `confirm/prompt` → модалки, БД-агрегати замість limit:9999/cap-20k. **Міграції 9, 10, 11** — власник виконав (повна).
   - ⚠️ **Власнику (опц.):** задати `MANAGER_PASSWORD` в env Cloudflare, якщо потрібен окремий вхід оператора без доступу до грошей/налаштувань. Без нього працює лише власник (як раніше).

## 🎯 СТАТУС РОБІТ
~~Зображення~~ ✅ · ~~Адмінка 3.0~~ ✅ · ~~ТЗ черга 2 (П0+П1)~~ ✅ · ~~Юр-частина~~ ✅ · ~~Глибокий аудит~~ ✅
**Чекає власника:** ✅ УСЕ ВИКОНАНО на проді (міграція, webhook, EmailJS, env, smoke-тест — 2026-06-28). Свідомо відкладено власником: `config.legal`/`payment` (пізніше), WAF (платна, нема доступу), `MANAGER_PASSWORD` (опц.). Деталі та наступний беклог — у **🚦 START HERE** вгорі файлу.

---

## 🗂️ КАРТА ПРОДУКТІВ ✅ (рішення власника 2026-06-11: нова лінійка, ціни ті самі)
| ключ | Назва | Фото | Ціна | Пакування | Фото-папка |
|------|-------|------|------|-----------|------------|
| `small`  | Маленький «Момент» | 12        | 400₴ | конверт          | envelopes-01 |
| `medium` | Подвійний «Спогад» | 24        | 600₴ | 2 конверти       | envelopes-04 |
| `large`  | Великий «Архів»    | 50–80 (cap 80) | 900₴ | коробка          | box-01 |
| `baby`   | «Малюк»            | 12 (опц. подвійний) | 450₴ | конверт-ведмедик | newborn-05 |

Оновлено й синхронізовано: `config.js` (packages + photoCap з `cap`), `functions/_utils.js` (PACKAGES + PRODUCT_NAMES), `index.html` (теги/описи карток), `order.html` (PRODUCT_DISPLAY + cap-тост), `account.html` (SETS+PKG_NAMES), `admin.html` (select+PRODUCT_LABEL).
Auto-upgrade std: 12→24→50(до 80). baby cap 12.
**Лишилось (відкрите):** ціна великого 50–80 — поки фікс 900₴; модель доплати за фото понад 50 вирішимо при налаштуванні цін в адмінці. Опція «подвійний Малюк» — майбутня фіча.

---

## 📦 ЗАВДАННЯ

### A. Зображення ✅ (лишилось опційне)
- [x] Оптимізовано 25 PNG (57МБ) → `assets/products/` (envelopes/box/newborn, JPG q85, ≤1400px, 4.9МБ). Скрипт: PIL у `/tmp/optimize_imgs.py` (не в репо).
- [x] Мапа в `config.js` (`packages[].img`): small/medium→envelopes, large→box, baby→newborn.
- [x] `nabir.html` — мініатюра 64px у кожній картці.
- [x] `index.html` — hero (envelopes-01), 4 картки наборів, галерея «ЯК ВИГЛЯДАЮТЬ НАБОРИ» (8 реальних фото пакування, чесні підписи ПАКУВАННЯ/ДЕТАЛЬ — не фейк-кадри клієнтів, конституція 8.5 ✓).
- [x] `account.html` «Проявити» — мініатюри 52px (CSS `.pv-set-thumb` у cabinet.css).
- [ ] (опц.) `order.html` — прев'ю обраного набору в контекст-банері.
- [x] `товар_фото/` лишається в `.gitignore`; у репо — лише `assets/`.
- Відео `Ньюборн/*.mp4` (57МБ разом) поки НЕ використано — кандидат на hero-відео згодом.

### B. Адмінка (наступне; велике — фази)
Поточний стан: `admin.html` (790 рядків) = логін + таблиця замовлень (фільтри статусів) + фотографи. API: `/api-orders`, `/api-photographers`, `/api-admin-login`.
Бекенд-таблиці вже є (міграція): `expenses`, `inventory`, `inventory_log`, `settings`, `orders`(+cost/paid_at/shipped_at), `clients`, `order_photos`, `photographers`(+tg).
Зразок функціоналу — CopyLounge (`_reference/copylounge/.../public/index.html`, 1292 р.): вкладки home(швидкий продаж/чернетки/нагадування/шаблони), sales, expenses, clients, stock, cash(звірка/залишки), dash(періоди/net), charts.

Фази:
- [x] B0. Каркас — нова темна на-бренд SPA `admin.html` (tokens.css), сайдбар-навігація з бейджами (нові замовлення / low-stock), мобільна нижня панель.
- [x] B1. Замовлення 2.0 — таблиця (фільтри статусів + пошук), повна картка-модалка: таймлайн статусу, всі поля, **кадри з R2** (`api-admin-photos` + лайтбокс), зміна статусу/суми/ТТН/нотаток, magic+статус-лінки, створення замовлення.
- [x] B2. Дашборд **2.0 (графіки + ієрархія)** — KPI-картки з кольоровими акцент-бордюрами/іконками/трендами (% проти минулого періоду), середній чек; графіки (SVG, без бібліотек): стовпчики «виручка по місяцях», donut «канали», горизонт. бари «лійка» (клік→фільтр) + «витрати за категоріями»; лідерборд «топ фотографи»; стрічка «остання активність»; ціль-бар із фікс-витратами; алерт low-stock. Нові CSS-компоненти: `.kpi/.panel/.bars/.hbar/.donut/.lb-row/.act-row`.
- [x] B3. Витрати — форма + таблиця + категорії + видалення (`api-expenses` вже був готовий).
- [x] B4. Склад — позиції, low-stock підсвітка, +/−/±N рух, додати/видалити (`api-inventory` вже був).
- [x] B5. Каса — стартові залишки (settings), дохід/витрати/очікувано на руках, розбивка готівка/картка.
- [x] B7. Налаштування — цілі/фінмодель (settings), ціни read-only з config.js, експорт CSV (замовлення/витрати).
- [x] Фотографи — портовано (CRUD, пароль, комісія, активність) у новий дизайн.
- **Бекенд:** новий `api-admin-photos.js`; `api-expenses`/`api-inventory`/`api-settings` уже існували (зроблені раніше) — перевикористано.
- [x] **Адмінка 3.0 — рескін за `_reference/Адмін.html` (професійний рівень).** Темна панель зі scanline-текстурою, sidebar (mono-нав + italic-лічильники + safelight-статус), stats із hairline-сіткою + italic Cormorant-числа, брендові бейджі статусів, поетичні заголовки («№ 0X · …» + italic). **Drawer** (висувна панель справа) замість модалки для деталей замовлення: таймлайн стадій, статус-селект, R2-кадри, ТТН/сума/комісія, нотатки, лінки, sticky-футер. Бекенд: новий `api-clients.js`.
- [x] B6. **Клієнти** — вкладка з акаунтами (`api-clients`): stats (акаунтів/повторні/виручка/LTV) + таблиця (замовлень, витратив, з нами з).
- [x] **Виплати партнерам** — у вкладці Партнери: нарахована комісія (з оплачених замовлень × %), кнопка «Виплатити» → логує витрату «Комісія фотографу». Stats: виторг через партнерів, комісій нараховано.
- [x] **Швидке просування статусу** — кнопка «→» у рядку таблиці замовлень (без відкриття drawer).
- [x] **Аналітика дашборду** — конверсія (paid/всі), час виконання (створення→відправка), покриття фікс-витрат, у роботі зараз.
- [ ] B8. «Нічого не пропускати» — окрема черга замовлень, що впали в БД (зараз: TG-алерт у api-retail + recovery-retry; constraint 4c усуває причину). Опційно: таблиця failed_orders + вкладка.

---

## ✅ ЗРОБЛЕНО (цей цикл)
- **Кабінет клієнта** `account.html` — SPA (home/архів/проявити/профіль/деталь), лайтбокс, плівкові стрічки з реальних R2-фото, стрічка подій, фільтри тонів. Бекенд: `api-client-auth`, `api-client-orders`, `api-client-photos` (HMAC-підписані URL). Реєстрація на екрані успіху `order.html` + автоприв'язка історії за телефоном.
- **R2-архів фото**: `send-order.js` кладе кожен кадр у R2 + `order_photos` ДО Telegram (нічого не губиться).
- **🔴 Критичний фікс**: `orders_source_check` валив усі вставки → порожній кабінет + мертве посилання статусу. Міграція 4c перевизначає constraint. + 4b: product_type nullable. **Власник: виконати секцію 4c/7 у Supabase SQL Editor.**
- **Фікс гонки фото**: `saveDraft()` одразу при додаванні фото (не лише після валідації) — закриття вкладки під час завантаження більше не губить кадри.
- Logged-in клієнт: нове замовлення лінкується до акаунта (Bearer → `api-retail` ставить `client_id`); на успіху — кнопка «відкрити кабінет».

---

## ⚠️ ДІЇ ВЛАСНИКА (не блокувати розробку, але треба)
- [ ] Supabase SQL Editor → виконати міграцію секції **4b/4c/6/7** (`migrations/proyav_admin.sql`). 4c — критично, інакше замовлення падають.
- [x] R2 bucket `proyav-photos` + binding `PHOTOS` (зроблено).
- [ ] Підтвердити лінійку товарів (назви/кількості/ціни) — див. «Карта продуктів».

---

## 🧠 ВАЖЛИВІ НЮАНСИ (щоб не наступити двічі)
- `orders` та її CHECK-обмеження живуть **лише в Supabase**, не в репо. Додаєш нове значення `source`/`status`/`product_type` у коді — спершу онови constraint у БД, інакше insert падає мовчки.
- Реальні колонки БД: `total_amount`(не amount), `ttn`(не np_ttn), `qty_total`(не print_count), `client_name`/`client_phone`, `token`, `client_id`. Статуси: `new/uploaded/in_progress/sent/paid` (накладний → `paid` термінальний).
- Ціни — лише `config.js` + дзеркало `functions/_utils.js`. Не вигадувати, не дублювати.
- Токени/секрети (TG_TOKEN, JWT_SECRET, SUPABASE_SERVICE_KEY, EmailJS) — лише env, ніколи в клієнті.
- Після тестів на локальному сервері — **зупиняти його** (`preview_stop`), власник тестує на проді.
- Лок. прев'ю: launch config `static` (python http.server :8123).
- **Cloudflare Pages: ліміт 25 МБ на файл.** Важкі архіви/медіа НЕ комітити. Усі `*.zip` у `.gitignore`. Якщо важкий файл уже закомічено — `.gitignore` не досить, треба `git rm --cached <файл>` + новий коміт (Pages деплоїть дерево останнього коміту, не історію). Уникати `git add -A` біля важких файлів. (Інцидент: `copylounge-main.zip` 43МБ потрапив у 317b5a1 → деплой падав; прибрано в aa029c2.)
